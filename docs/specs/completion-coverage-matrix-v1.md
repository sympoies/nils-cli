# Completion Coverage Matrix v1 (Workspace Binaries)

## Policy notes

- Inventory source of truth: `bash scripts/workspace-bins.sh`.
- Obligation rule for this matrix: all workspace binaries are `required` unless explicitly `excluded` as internal/example binaries.
- Explicit exclusion: `cli-template` is `excluded` because it is an example/template CLI and is out of scope for user-facing release
  contract migration (rollout plan retired; see repository history).
- Explicit exclusion: `agent-runtime` is `excluded` because the binary has no `completion <bash|zsh>` export or generated shell assets
  yet. Its runtime-kit render / install / doctor / audit command surface is implemented; this row flips to `required` when the crate adds
  a clap-first completion subcommand and checked-in completion assets.
- Explicit treatment: `image-processing` is `required` (user-facing CLI in `README.md`) and must ship clap-first thin completion adapters in
  both shells.
- Alias policy: alias families are required only for `git-scope` (`gs*`), `git-cli` (`gx*`), `codex-cli` (`cx*`), and `fzf-cli` (`fx*`) per
  `AGENTS.md`; other binaries have no alias requirement.
- Completion quality baseline for every `required` binary: subcommands + long/short flags + declared value candidates.
- Completion quality baseline for every `required` binary also requires context-aware filtering by cursor position (not global candidate
  dumps).
- Preferred implementation mode for every `required` binary: clap-first (`clap` + `clap_complete`) generated baseline completions, with thin
  shell adapters and optional dynamic value extensions only when needed.
- Runtime completion-mode toggles are not supported; adapters must stay clap-first and fail closed when generated completion cannot be
  loaded.
- Completion enforcement metadata is required for every `required` binary and must use:
  `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`.
- Completion engine dimension: the enforcement metadata may carry an optional `completion_engine=<static|dynamic>` key. When the key is
  absent, `static` is assumed — the CLI ships a `clap_complete` `generate()`-baked baseline (the default; every current row is `static`). A
  `dynamic` CLI opts into the `clap_complete` `CompleteEnv` runtime-completion engine (`unstable-dynamic`) and must declare
  `completion_engine=dynamic` explicitly. Dynamic mode extends the clap-first baseline and remains a single completion path per CLI (it is
  not an alternate dispatch): the completion-freshness audit classifies a `dynamic` CLI as a runtime adapter (no static baseline to diff).
  Flag-parity skips a `dynamic` CLI (no static flag list to diff) and the shared adapter loads the `CompleteEnv` registration shape via its
  dynamic loader helper. `git-cli` is the first `dynamic` CLI (its `worktree go`/`remove` complete live worktree names/branches). Until a CLI
  declares `completion_engine=dynamic`, every audit path is unchanged. See `docs/runbooks/cli-completion-development-standard.md`.
- This matrix currently tracks asset coverage, obligation decisions, and completion enforcement metadata; deeper quality/export-command
  conformance is validated in rollout sprints and completion tests.

## Matrix

| Binary                | Obligation | Zsh completion (`completions/zsh`) | Bash completion (`completions/bash`) | Alias requirement                                              | Completion enforcement metadata                                                                                                                                         | Rationale                                                                                                                                                                                                                                                    |
| --------------------- | ---------- | ---------------------------------- | ------------------------------------ | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `agent-docs`          | `required` | `present` (`_agent-docs`)          | `present` (`agent-docs`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `agent-hook`          | `required` | `present` (`_agent-hook`)          | `present` (`agent-hook`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Shared cross-provider hook policy control plane; completion is exported by `agent-hook completion <bash\|zsh>` and generated from clap metadata.                                                                                                             |
| `agent-memory`        | `required` | `present` (`_agent-memory`)        | `present` (`agent-memory`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed; completion_engine=dynamic` | Workspace binary; completion is exported by `agent-memory completion <bash\|zsh>`; the `SCOPE` argument completes live memory scopes (`root`, `global`, `agents/<id>`, `personas/<id>`, `profiles/<id>`, `candidates/<id>`) via clap_complete `CompleteEnv`. |
| `agent-out`           | `required` | `present` (`_agent-out`)           | `present` (`agent-out`)              | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `agent-run`           | `required` | `present` (`_agent-run`)           | `present` (`agent-run`)              | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `agent-run completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                     |
| `agent-runtime`       | `excluded` | `missing`                          | `missing`                            | not required                                                   | `not required (excluded)`                                                                                                                                               | Runtime-kit binary has no completion export or generated shell assets yet; keep excluded until clap-first completion support lands.                                                                                                                          |
| `agent-session`       | `required` | `present` (`_agent-session`)       | `present` (`agent-session`)          | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `agent-session completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                         |
| `main-agent`          | `required` | `present` (`_main-agent`)          | `present` (`main-agent`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Typed Main Agent orchestration facade; completion is exported by `main-agent completion <bash\|zsh>` and generated from clap metadata.                                                                                                                       |
| `agent-scope-lock`    | `required` | `present` (`_agent-scope-lock`)    | `present` (`agent-scope-lock`)       | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `agent-scope-lock completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                      |
| `api-gql`             | `required` | `present` (`_api-gql`)             | `present` (`api-gql`)                | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `api-grpc`            | `required` | `present` (`_api-grpc`)            | `present` (`api-grpc`)               | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `api-rest`            | `required` | `present` (`_api-rest`)            | `present` (`api-rest`)               | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `api-test`            | `required` | `present` (`_api-test`)            | `present` (`api-test`)               | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `api-websocket`       | `required` | `present` (`_api-websocket`)       | `present` (`api-websocket`)          | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `browser-session`     | `required` | `present` (`_browser-session`)     | `present` (`browser-session`)        | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `browser-session completion <bash\|zsh>` and generated from clap metadata.                                                                                                                               |
| `canary-check`        | `required` | `present` (`_canary-check`)        | `present` (`canary-check`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `canary-check completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                  |
| `cli-template`        | `excluded` | `missing`                          | `missing`                            | not required                                                   | `not required (excluded)`                                                                                                                                               | Explicitly treated as internal/example template CLI and out of scope for user-facing release contract migration.                                                                                                                                             |
| `claude-cli`          | `required` | `present` (`_claude-cli`)          | `present` (`claude-cli`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `claude-cli completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                            |
| `codex-cli`           | `required` | `present` (`_codex-cli`)           | `present` (`codex-cli`)              | required (`cx*`) and present in `aliases.zsh` + `aliases.bash` | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Alias family is mandated by policy; completion files exist in both shell directories.                                                                                                                                                                        |
| `devlog`              | `required` | `present` (`_devlog`)              | `present` (`devlog`)                 | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `devlog completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                                |
| `docker-tools`        | `required` | `present` (`_docker-tools`)        | `present` (`docker-tools`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `docker-tools completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                          |
| `docs-impact`         | `required` | `present` (`_docs-impact`)         | `present` (`docs-impact`)            | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `docs-impact completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                   |
| `forge-cli`           | `required` | `present` (`_forge-cli`)           | `present` (`forge-cli`)              | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `forge-cli completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                             |
| `fzf-cli`             | `required` | `present` (`_fzf-cli`)             | `present` (`fzf-cli`)                | required (`fx*`) and present in `aliases.zsh` + `aliases.bash` | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Alias family is mandated by policy; completion files exist in both shell directories.                                                                                                                                                                        |
| `gemini-cli`          | `required` | `present` (`_gemini-cli`)          | `present` (`gemini-cli`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `git-cli`             | `required` | `present` (`_git-cli`)             | `present` (`git-cli`)                | required (`gx*`) and present in `aliases.zsh` + `aliases.bash` | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed; completion_engine=dynamic` | Dynamic pilot: `worktree go`/`remove` complete live worktree names/branches via clap_complete `CompleteEnv`; alias family mandated by policy; completion files exist in both shell directories.                                                              |
| `git-lock`            | `required` | `present` (`_git-lock`)            | `present` (`git-lock`)               | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `git-scope`           | `required` | `present` (`_git-scope`)           | `present` (`git-scope`)              | required (`gs*`) and present in `aliases.zsh` + `aliases.bash` | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Alias family is mandated by policy; completion files exist in both shell directories.                                                                                                                                                                        |
| `git-summary`         | `required` | `present` (`_git-summary`)         | `present` (`git-summary`)            | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `github-app-cli`      | `required` | `present` (`_github-app-cli`)      | `present` (`github-app-cli`)         | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `github-app-cli completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                        |
| `heuristic-inbox`     | `required` | `present` (`_heuristic-inbox`)     | `present` (`heuristic-inbox`)        | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `heuristic-inbox completion <bash\|zsh>` and generated from clap metadata.                                                                                                                               |
| `evidence`            | `required` | `present` (`_evidence`)            | `present` (`evidence`)               | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Skill-usage evidence archive CLI; completion is exported by `evidence completion <bash\|zsh>` and generated from clap metadata.                                                                                                                              |
| `image-processing`    | `required` | `present` (`_image-processing`)    | `present` (`image-processing`)       | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Explicitly treated as a user-facing CLI (not internal/example); completion is exported by `image-processing completion <bash\|zsh>` and loaded through thin fail-closed shell adapters.                                                                      |
| `macos-agent`         | `required` | `present` (`_macos-agent`)         | `present` (`macos-agent`)            | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `md-render`           | `required` | `present` (`_md-render`)           | `present` (`md-render`)              | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Shared Markdown template renderer; completion is exported by `md-render completion <bash\|zsh>` and generated from clap metadata.                                                                                                                            |
| `memo`                | `required` | `present` (`_memo`)                | `present` (`memo`)                   | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `model-cross-check`   | `required` | `present` (`_model-cross-check`)   | `present` (`model-cross-check`)      | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `model-cross-check completion <bash\|zsh>` and generated from clap metadata.                                                                                                                             |
| `opencode-cli`        | `required` | `present` (`_opencode-cli`)        | `present` (`opencode-cli`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `opencode-cli completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                          |
| `repo-retro`          | `required` | `present` (`_repo-retro`)          | `present` (`repo-retro`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `repo-retro completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                    |
| `plan-archive`        | `required` | `present` (`_plan-archive`)        | `present` (`plan-archive`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `plan-archive completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                          |
| `plan-issue`          | `required` | `present` (`_plan-issue`)          | `present` (`plan-issue`)             | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Rust orchestration CLI entrypoint; completion is exported by `plan-issue completion <bash\|zsh>` and loaded through thin fail-closed shell adapters.                                                                                                         |
| `plan-issue-local`    | `required` | `present` (`_plan-issue-local`)    | `present` (`plan-issue-local`)       | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Local rehearsal binary paired with `plan-issue`; completion is exported by `plan-issue-local completion <bash\|zsh>` and loaded through thin fail-closed shell adapters.                                                                                     |
| `plan-tooling`        | `required` | `present` (`_plan-tooling`)        | `present` (`plan-tooling`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `screen-record`       | `required` | `present` (`_screen-record`)       | `present` (`screen-record`)          | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `secrets`             | `required` | `present` (`_secrets`)             | `present` (`secrets`)                | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed; completion_engine=dynamic` | Workspace binary; completion is exported by `secrets completion <bash\|zsh>`; the `name` argument (`pull`/`edit`/`which`) completes live store entry names via clap_complete `CompleteEnv`.                                                                  |
| `semantic-commit`     | `required` | `present` (`_semantic-commit`)     | `present` (`semantic-commit`)        | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion files exist in both shell directories.                                                                                                                                                                                          |
| `skill-usage`         | `required` | `present` (`_skill-usage`)         | `present` (`skill-usage`)            | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `skill-usage completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                   |
| `test-first-evidence` | `required` | `present` (`_test-first-evidence`) | `present` (`test-first-evidence`)    | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `test-first-evidence completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                   |
| `web-evidence`        | `required` | `present` (`_web-evidence`)        | `present` (`web-evidence`)           | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `web-evidence completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                          |
| `zsh-kit`             | `required` | `present` (`_zsh-kit`)             | `present` (`zsh-kit`)                | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Workspace binary; completion is exported by `zsh-kit completion <bash\|zsh>` and generated from clap metadata.                                                                                                                                               |
| `review-evidence`     | `required` | `present` (`_review-evidence`)     | `present` (`review-evidence`)        | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `review-evidence completion <bash\|zsh>` and generated from clap metadata.                                                                                                                               |
| `review-specialists`  | `required` | `present` (`_review-specialists`)  | `present` (`review-specialists`)     | not required                                                   | `completion_mode=clap-first; completion_mode_toggles=forbidden; alternate_completion_dispatch=forbidden; generated_load_failure=fail-closed`                            | Agent workflow primitive; completion is exported by `review-specialists completion <bash\|zsh>` and generated from clap metadata.                                                                                                                            |

## Exclusion summary

- `excluded`: `cli-template` (explicit staged exclusion), `agent-runtime` (runtime-kit binary without completion export/assets yet).

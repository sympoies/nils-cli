# Development log - 2026-09

## 2026-09-15 - Folded the review-loop and pending-review plumbing into the commands

### Result

- Added `pr review-loop observe --auto-state` and `--preflight`: the CLI reads
  the chain tip it compare-and-swaps against, and runs the full `--dry-run`
  verdict sweep in front of a live append, failing as `review_preflight_failed`
  with every failing rule named.
- Added `pr review --recover-pending`: on `github_pending_review_exists` the
  exact abandoned viewer-owned pending review is deleted through the existing
  guarded delete, then the submission proceeds.

### Why / context

- Consumers were re-implementing five-call compare-and-swap plumbing around the
  review-loop primitives in bash, so the ledger's guarantees were enforced by
  prose in a Markdown skill rather than by the CLI. sympoies/agent-runtime-kit
  carries the same 283-line fence in three delivery skills.
- The pending-review recovery mattered more than the line count: it guards a
  destructive delete, and the guard was caller-side `jq`. A defect there
  deletes the wrong review, so the safety argument for an unundoable operation
  should not live in a prompt. Recovery now enters the same guarded-delete
  path, lease and read-back as `pr pending-review delete --confirm-abandoned` —
  one copy, not two.
- `--auto-state` is deliberately weaker than `--expected-state` and is
  documented as such. A digest the caller carries from an earlier round is a
  claim about what it already saw, and that claim is what catches a resumed
  shell reusing genesis state; no self-read can reconstruct it. The one window
  the flag can still see — a tip that moves between the preflight sweep and the
  append — fails closed as `review_state_tip_moved` naming both digests, never
  a silent re-read, because the transition was computed against the chain the
  sweep read.

### Evidence

- Five recovery cases in tests/integration/pr_review.rs pin the
  delete-then-submit order, the opt-in (no flag still fails closed), and three
  refusals: body mismatch, ambiguous candidate, and missing --submit-review.
  Five unit cases in ops/pr_review_loop.rs pin the auto-state append, the
  unchanged behaviour without it, the multi-rule preflight failure, and a
  concurrent append injected between the sweep and the append.

### Links

- sympoies/nils-cli#1740 the request; sympoies/agent-runtime-kit#140 the
  consumer's context-budget tracker

## 2026-09-15 - devlog repairs a log instead of asking someone to

### Result

- Added `devlog fix`: of the eleven problems `check` reports, six have exactly
  one correct repair, and it applies those and reports the rest. Thirteen logs
  in this organization predate the section contract and every one of them
  failed `check`; the alternative was a 259-line script in a scratch directory
  that three more repositories would have depended on.
- Gave every reader one fence rule to index. An entry heading or a section
  heading quoted in a code block is an example, so `check` no longer counts it
  and `fix` no longer rewrites it.
- Fixed what `new` writes: a bare URL is now an autolink, and a path outside
  the repository root no longer gains a second leading slash.

### Why / context

- The conventions were enforceable but not repairable, so adopting the
  capability in a repository with history meant hand-editing months of it —
  which is the care the crate exists to remove.
- Making `check` and `fix` share the fence rule was not a tidiness choice.
  Leaving `fix` merely fence-aware meant the two disagreed about what an entry
  is, and the first fenced test failed by writing a generated section into the
  middle of a code block rather than by leaving the example alone.

### Evidence

- The algorithm was written twice — once as a port, once in Rust — and both
  were run over untouched copies of the 763 entries in the thirteen failing
  logs. All thirteen end clean, and the two implementations agree byte for byte
  except where this deliberately does better.
- Four reviewers raised 26 findings across the two changes. Three were the same
  mistake: repairing the instance reported without looking for its siblings.
  The worst was an infinite loop that exhausted memory on an entry ending in an
  unterminated fence, in a command about to rewrite thirteen repositories.
- Rules were measured rather than assumed. 3105 of 3136 standalone bold lines
  are template labels; the other 31 include `**Why**` and `**Follow-up**`,
  which a prefix match would have turned into sections nobody wrote.

### Links

- <https://github.com/sympoies/nils-cli/pull/1736>
- <https://github.com/sympoies/nils-cli/pull/1735>

### Follow-ups

- Nothing reports an unterminated fence, which hides every problem below it
  (#1737).
- No repository gates its log, so a migrated one drifts back unnoticed;
  `dsh-runtime-kit` already has.

## 2026-09-15 - The macOS session-lock flake was an ENOENT that could not be true

### Result

- Both session record lock opens retry a spurious ENOENT, bounded at eight
  attempts and about 25ms, and only ENOENT.
- check requires Result, Why / context and Evidence; Links and Follow-ups are
  optional, and an empty one is omitted rather than filled with a placeholder.

### Why / context

- openat with O_CREAT may not report ENOENT when the parent directory exists,
  and the lock path validates that parent's device and inode immediately before
  the call. macOS returns it anyway when two processes create the same lock
  file at once, so the loser of a concurrent metadata attach failed with
  session-record-lock-open-failed instead of the revision conflict the contract
  promises.
- The first fix covered only the openat path the failing tests happened to
  exercise. The race is between processes rather than between call sites, so
  the OpenOptions path behind delete, coordination, the mailbox and every
  mutate_session_record caller needed the same policy.
- Links was required because the first cut of the crate inferred it from one
  backfill whose entries all carried links. Measured against 483 hand-written
  entries across the organization, 60 have none, so the requirement was wrong
  rather than the logs.

### Evidence

- The diagnostics in #1725 printed the losing worker's message on the next
  occurrence: session storage failed at <state>/session-locks/<id>.lock: No
  such file or directory.
- Four mutants, each killed by exactly one test: retry every error, bound eight
  to sixteen, and the sibling path losing the retry. Shrinking the bound is
  deliberately not caught, because the test budgets derive from it.
- devlog check exits 0 across six logs and 616 entries, where five of them
  exited 65 before.

### Links

- <https://github.com/sympoies/nils-cli/pull/1729>
- <https://github.com/sympoies/nils-cli/pull/1727>
- <https://github.com/sympoies/nils-cli/issues/1712>

### Follow-ups

- #1712 stays open until macOS CI has been quiet long enough to count as proof;
  retries = 1 means a still-broken lane reports flaky and then green.

## 2026-09-14 - devlog CLI refuses unresolved merge conflicts

### Result

- devlog check reports a new conflict-markers problem kind and stops parsing
  that file; devlog new and devlog index refuse it without writing, and new
  checks the month file and the index before touching either.

### Why / context

- A month file conflicts structurally: every entry is inserted at the same
  position under the month heading, so two branches adding an entry in the same
  month always collide. Both sides of the conflict are well-formed entries, so
  the parser counted them as real content and reported the file as healthy at
  exit 0, while new stacked a fresh entry on top of the markers.

### Evidence

- Seven regression tests failed against the unfixed source (cargo test -p
  nils-devlog --test integration cli:: at exit 101, check printing "3 entries"
  and "ok: no structural problems" for a conflicted fixture); 60 tests pass
  after the fix, and the full CI-parity stack passed at exit 0.

### Links

- PR #1719
- Issue #1711 for the CLI itself

### Follow-ups

- devlog search still returns lines from both sides of an unresolved conflict;
  it is read-only, so it was left out of this change.

## 2026-09-14 - Enabled the development log and gave it a CLI owner

### Result

- Enabled `docs/devlog/` and backfilled 88 entries covering 2026-01 through
  2026-09, written from the 2422 commits on `main` and weighted by milestone
  rather than by month.
- Added the `devlog` CLI (`new`, `search`, `check`, `index`, `completion`),
  detecting the log at `docs/devlog` or `docs/source/devlog` so both
  organization conventions work without configuration.

### Why / context

- The conventions were prose with no enforcement. A mis-named month file is
  invisible to a glob and absent from the index, the index drifts from the
  tracked files, and entry ordering is positional - none of which any audit
  inspected.
- `MD036` in the Markdown lint baseline rejects bold section labels, so
  hand-written entries using the upstream bold-label template fail the docs
  lane. The first lint run on the backfill reported 352 such violations;
  `devlog new` now emits level-three headings by construction.
- A per-repo shell helper was the first implementation, and review found three
  defects in 47 lines: `--help` searched as a literal term, an unbound-variable
  failure under inherited `nullglob`, and a misleading diagnostic for an absent
  month. Five repositories already carried copies of that helper.

### Evidence

- All 420 cited commit hashes resolve in this repository and all 170 hash-to-PR
  pairings match their commit subject; both checks were scripted joins against
  `git log` and caught two incorrect hashes.
- `devlog check` found a real ordering defect in the backfill after review had
  passed: the 2026-04 entries had 04-15 above 04-17. No reviewer and no audit
  inspects entry ordering, which is the gap the CLI closes.
- Dogfooding this entry caught a second defect: the first `devlog new` emitted
  unwrapped bullets that failed MD013, so bullets now wrap at column 79 with
  two-space continuations.
- 29 behavioral tests cover every documented exit code, both devlog paths,
  subdirectory invocation, leap-year dates, and a round trip asserting `check`
  accepts what `new` produces.

### Links

- #1710 enabled the log and landed the backfill
- #1711 tracked this CLI
- sympoies/agent-runtime-kit#120 makes the devlog a default agent capability
- sympoies/agent-runtime-kit#121 consolidates per-repo devlog docs into the
  agent home

### Follow-ups

- #1712 tracks the `agent-session` `metadata::concurrent_*` failures that
  blocked this delivery on the macOS coverage job.

## 2026-09-14 - Hardened the dependabot apply workflow's pull-request selection

### Result

- Both jobs in `dependabot-third-party-apply.yml` now select the bump's pull request with
  `--jq '[.[] | select(.isCrossRepository == false)][0]'` instead of taking `.[0]`, so a
  same-named branch in a fork can no longer be chosen.
- The workflow-level concurrency group is scoped to the verified commit
  (`workflow_run.head_sha`) rather than the branch.

### Why / context

- `gh pr list --head <name>` filters on the branch name regardless of which repository the
  branch lives in, and this repository is public with forks. An outside user could open a
  pull request from a fork branch named like a Dependabot branch, become the first element
  of the result, and fail the `dependabot[bot]` author check — turning the privileged
  commit job red and stranding that bump's artifact refresh. It is an availability problem,
  not a privilege one: the author and head-SHA gates still hold.
- The qualification cannot move into the flag. `gh pr list` documents that the
  `<owner>:<branch>` form is *not supported*; passing it matches nothing, which silently
  turns the whole automation into a no-op. That was verified the hard way in the sibling
  `nils-alfredworkflow` repository, where exactly that "fix" shipped and left its
  automation inert while every run stayed green.
- GitHub keeps one pending run per concurrency group and cancels the previously pending one
  when a newer run queues. With the group keyed by branch, a stale event arriving after a
  useful merge trigger could cancel it, leaving a green bump unmerged with nothing
  reporting a failure. Keying by the verified commit removes that interaction; the commit
  job's non-force ref update is what actually serializes writes.

### Evidence

- `gh pr list --repo sympoies/nils-alfredworkflow --head sympoies:docs/enable-devlog-backfill
  --state all` returns nothing, while the same query with the bare branch name returns a
  pull request — the reason the owner-qualified form is not used here.
- The `isCrossRepository` field is returned by `gh pr list --json` and filters correctly in
  the same query shape used by both jobs.
- `bash scripts/ci/nils-cli-checks-entrypoint.sh --local-fast` passes on the change.

### Links

- <https://github.com/sympoies/nils-alfredworkflow/pull/238>

## 2026-09-13 - Bounded coordination receipts by aggregate bytes

### Result

- Added a 4 MiB per-principal budget over the serialized size of retained
  idempotency receipts, rejecting at the limit rather than evicting.
- Added Codex account failover: an account-aware auto-resume recovery policy,
  a switch to the next capacity-confirmed account before a resume, and an
  effective default account resolved and projected at launch.

### Why / context

- Count quotas bounded how many receipts a principal could retain but not how
  large they could be, so a principal could hold a small number of very large
  receipts and exhaust the store anyway. A byte budget closes the dimension the
  count quota left open.
- Rejecting at the limit rather than evicting is the important half. Evicting a
  retained idempotency key makes a later replay of that key look like a fresh
  request, which turns an overflow into a duplicate-execution bug - a strictly
  worse failure than a refusal.

### Evidence

- `cb6f6657` (#1706) the byte budget with its reject-not-evict reasoning,
  `6fd0de9c` (#1702) the account failover.

### Links

- `crates/agent-session/docs/README.md`

## 2026-09-13 - Lowered the CI retry budget on measured evidence

### Result

- Lowered nextest `retries` from 2 to 1 in the `ci` profile, replacing an
  earlier mitigation note with the measured basis for the new budget, and
  recorded the clusters still flaking.

### Why / context

- The decision was made from data, not intuition: retry depth was measured
  across 74 job-level JUnit files from two `main` windows, and all 29 flaky
  events passed on the first retry. The second attempt therefore never changed
  an outcome while costing wall-clock on every genuine failure.
- This pairs with `946a3157` (#1649), which enabled `flaky_summary` on both
  JUnit publish steps so a retried pass appears in the check people actually
  read. Without that visibility, reducing the budget would have been a guess;
  with it, masked flakes are countable.

### Evidence

- `70ad9b47` (#1704) carries the measurement in its commit body.

### Links

- `.config/nextest.toml` `ci` profile

## 2026-09-12 - Added earned rate-limit resets to codex-cli

### Result

- Exposed reset-credit counts in diagnostics with aligned text output, added
  confirmed idempotent reset redemption for ChatGPT accounts, and documented
  and tested the stable JSON and completion contracts.
- Added DSH provider history to `agent-session`.

### Why / context

- Redemption is idempotent and confirmed because a reset is a consumable: a
  retried redemption that spends a second credit is unrecoverable, so the
  operation is designed to be safe under the retries an agent will perform.

### Evidence

- `b0d30674` (#1693) the reset credits, `ddae2eea` (#1695) the DSH history.

### Links

- `crates/codex-cli/docs/` diagnostics contracts

## 2026-09-10 - Refused literal input to a blocked agent session

### Result

- A session whose turn phase is `needs_input` now refuses input carrying
  literal text with `agent-blocked` (HTTP 409) before it reaches the pane,
  across `send` and the structured prompt endpoints.
- Made long-session retitle durable and added live-capacity account selection
  for retitles.

### Why / context

- When a provider approval or question dialog owns the pane, text delivered
  there lands in the dialog - one keystroke from choosing a highlighted option.
  The endpoints could therefore approve something nobody agreed to. This is a
  consent defect, not an ergonomics one, which is why the refusal happens
  before anything reaches the pane rather than being handled downstream.

### Evidence

- `e37fc8c0` (#1665) states the dialog-ownership reasoning; `79fb37c7` (#1662)
  and `7d82f5e3` (#1681) the retitle work.

### Links

- `f7c0ce4b` (#1624) feat(agent-session): add durable provider-neutral retitle
  v2

## 2026-09-09 - Typed the workspace lease protocol generation

### Result

- Typed the workspace lease protocol generation and bound a v2 `begin` to the
  call its target came from.

### Why / context

- An untyped generation is an integer that any caller can supply, which makes
  fencing only as strong as caller discipline. Typing it, and binding a `begin`
  to the originating call, means a lease operation cannot be reissued against a
  target it was not resolved for.
- This is the same defect class as the August default-branch flag removal: a
  guard that depends on the caller describing itself accurately is not a guard.

### Evidence

- `68f66fd2` (#1651) the typing, `0791a59a` (#1652) the call binding.

### Links

- `556067bd` (#1600) the v2 protocol these harden

## 2026-09-03 - Added the target-scoped workspace lease protocol v2

### Result

- Added `workspace-lease resolve`, which classifies one exact tool execution
  into zero or more canonical repository targets and owns no durable state.
  Structured `write`, `edit`, and `str_replace_editor` mutations resolve
  exactly; read-only forms, unknown tools, and arbitrary shell programs are
  handled distinctly.
- Tiered the DSH policy groups and added an advise rule mode.

### Why / context

- v1 leased the workspace; v2 leases the targets a specific execution will
  actually touch. That is what allows two agents to work the same checkout on
  disjoint files without contending, which the coarse lease made impossible.
- `resolve` owning no durable state is the design constraint that keeps it
  safe to call speculatively: classification is a pure function of the
  execution, so it can run on every tool call without accumulating anything.
- An arbitrary shell program cannot be resolved exactly, so it is classified as
  such rather than guessed at - the honest answer being the one that lets the
  caller decide.
- The advise rule mode exists so a policy can be observed before it is
  enforced, which is how a tier gets promoted on evidence rather than on
  expectation.

### Evidence

- `556067bd` (#1600) the protocol, `6674fd7c` (#1616) the policy tiers and
  advise mode.

### Links

- `8f6c59bd` (#1488) the August v1 lease authority

## 2026-09-02 - Streamed attachment uploads to disk

### Result

- Attachment uploads now enforce configurable declared and observed byte
  ceilings without buffering request bodies, publishing private temporary files
  only after a successful sync and cleaning them up on failure or
  cancellation, with coverage for streaming timing, admission ordering, exact
  limits, cleanup, and compatibility.
- Admitted `block-agent-artifact-routing` to the `agent-hook` v1 allowlist and
  added starring of history sessions.

### Why / context

- Both a declared and an observed ceiling are needed: the declared size is
  attacker-supplied and the observed size only becomes known while reading, so
  trusting either alone leaves a hole. Enforcing both without buffering means a
  hostile body is refused mid-stream rather than after it has been held in
  memory.
- Publishing only after a successful sync is what makes a partially-written
  attachment unobservable; cleanup on cancellation is what keeps a refused
  upload from leaving residue in the private temporary area.

### Evidence

- `7485243f` (#1594) the streaming upload, `bf80fae0` (#1599) the allowlist
  admission, `8f5d6eba` (#1602) history starring.

### Links

- `5f09ab10` (#1690) feat(agent-session): add bounded public metadata
  attachments

## 2026-09-01 - Added the typed agent-hook data-policy evaluator

### Result

- Added a deterministic data-policy boundary evaluating sensitive output,
  machine-local paths, and protected roots through a strict content-free JSON
  contract, with every decision bound to the exact execution identity and
  coverage for structured, streamed, binary, and replay cases.
- Added the provider session history API with latest-context paging.

### Why / context

- The contract is content-free by design: a policy evaluator that receives the
  content it is judging becomes a second copy of the sensitive data, and its
  logs, errors, and replay records become new leak surfaces. Evaluating over
  descriptors rather than payloads keeps the boundary from being the breach.
- Binding decisions to the exact execution identity is what makes a replayed
  decision detectable rather than silently reusable.

### Evidence

- `4b855b3d` (#1583) the evaluator, `bf459255` (#1584) and `a92d0221` (#1586)
  the history API and paging.

### Links

- `97261c63` (#683) the May machine-local path block this generalizes

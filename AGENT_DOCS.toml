[[document]]
context = "project-dev"
scope = "project"
path = "BINARY_DEPENDENCIES.md"
required = false
when = "always"
notes = "On-demand runtime dependency and degradation reference; read when a change adds, removes, invokes, installs, or diagnoses an external tool."

[[document]]
context = "project-dev"
scope = "project"
path = "docs/runbooks/cli-completion-development-standard.md"
required = false
when = "always"
notes = "On-demand completion and alias standard; read for completion assets, aliases, export paths, or shell integration."

[[document]]
context = "project-dev"
scope = "project"
path = "docs/runbooks/new-cli-crate-development-standard.md"
required = false
when = "always"
notes = "On-demand new CLI crate standard; read only when adding or substantially redesigning a user- or service-facing crate."

[[document]]
context = "project-dev"
scope = "project"
path = "docs/specs/crate-docs-placement-policy.md"
required = false
when = "always"
notes = "On-demand crate documentation placement policy; read when adding, moving, or reclassifying documentation."

[[document]]
context = "project-dev"
scope = "project"
path = "docs/runbooks/workspace-maintenance-reference.md"
required = false
when = "always"
notes = "On-demand setup, validation-lane, coverage, generated-artifact, and release/publish command reference."

[[document]]
context = "session-coordination"
scope = "project"
path = "crates/agent-session/docs/README.md"
required = true
when = "always"
notes = "Project-local index for the agent-session coordination CLI and protocol; work context is collision metadata, not work authorization."

# Finish-line validation contract: code edits in this workspace must run the
# local-fast CI gate before a project-dev task is declared done (agent-docs
# v0.30+). The Stop hook blocks finishing until this command has run since the
# last edit; waivable via AGENT_RUNTIME_VALIDATION_WAIVER=1.
[[validation]]
context = "project-dev"
commands = ["bash scripts/ci/nils-cli-checks-entrypoint.sh --local-fast"]
marker = ".cache/agent-validation/project-dev.ok"
description = "Run the nils-cli local-fast CI gate (fmt, clippy, workspace tests, docs/parity audits) before declaring a project-dev task done."

# Opt in to the project-local skill-name prefix lint (agent-docs v1.0+):
# `agent-docs audit` flags any `.agents/skills/<name>` directory whose name is
# not lowercase kebab-case starting with `project-` or `private-`.
[skills]
enforce_name_prefix = true

#compdef forge-cli

autoload -U is-at-least

_forge-cli() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_forge-cli_commands" \
"*::: :->forge-cli" \
&& ret=0
    case $state in
    (forge-cli)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-command-$line[1]:"
        case $line[1] in
            (provider)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__provider_commands" \
"*::: :->provider" \
&& ret=0

    case $state in
    (provider)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-provider-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
'--kind=[Provider implementation kind]:KIND:(forgejo)' \
'--base-url=[Absolute Forgejo base URL]:BASE_URL:_default' \
'--token-env=[Environment variable whose value supplies the provider token]:TOKEN_ENV:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':name -- Stable provider name used by `--provider`:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':name:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__provider__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-provider-help-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(pr)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__pr_commands" \
"*::: :->pr" \
&& ret=0

    case $state in
    (pr)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
'--head=[Source branch (defaults to the current branch)]:HEAD:_default' \
'--base=[Target / base branch (defaults to the repo'\''s default branch)]:BASE:_default' \
'--title=[PR / MR title (required)]:TITLE:_default' \
'(--body-file)--body=[PR / MR body / description text. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read PR / MR body from a file. Use \`-\` to read from stdin]:PATH:_default' \
'--kind=[PR / MR kind (selects branch-prefix rule). Required]:KIND:(feature bug chore docs ci refactor)' \
'*--reviewer=[Add a reviewer (repeatable)]:USER:_default' \
'*--label=[Add a label (repeatable)]:LABEL:_default' \
'--label-catalog=[Validate labels against this catalog when \`--strict-labels\` is set]:PATH:_default' \
'--test-first-evidence=[Directory holding a \`test-first-evidence\` record. Verified by the test-first gate when \`\[test_first\].require\` resolves true for a feature/bug PR. Requires a complete v2 record with a testable classification, durable pre-fix evidence, scoped final validation, a residual-gap declaration, and a bound baseline/delivery subject matching the current checkout]:DIR:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--no-draft[Open as ready-for-review instead of draft (default\: open as draft)]' \
'--strict-labels[Fail when selected labels are missing, not applicable, or conflict]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric id or branch name:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--state=[Filter by state (default\: open)]:STATE:(open closed merged all)' \
'--author=[Filter by author handle]:AUTHOR:_default' \
'--head=[Filter by head / source branch]:HEAD:_default' \
'--base=[Filter by target / base branch]:BASE:_default' \
'--limit=[Cap the number of returned PRs (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
'--title=[Replace the title (revalidates \`title_length\`)]:TITLE:_default' \
'(--body-file)--body=[Replace the body / description (revalidates body sections)]:BODY:_default' \
'--body-file=[Read replacement body from a file. Use \`-\` for stdin]:PATH:_default' \
'--base=[Re-target the PR / MR to this base branch]:BASE:_default' \
'*--add-label=[Add a label (repeatable)]:LABEL:_default' \
'*--remove-label=[Remove a label (repeatable)]:LABEL:_default' \
'*--add-reviewer=[Add a reviewer (repeatable)]:USER:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(comment)
_arguments "${_arguments_options[@]}" : \
'(--body-file)--body=[Comment body. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read comment body from a file. Use \`-\` for stdin]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(review)
_arguments "${_arguments_options[@]}" : \
'--decision=[Outcome decision to record in the comment metadata]:DECISION:((comments-only\:"Post an informational review outcome comment only"
approve\:"Record an approval outcome in the posted review comment"
request-changes\:"Record a request-changes outcome in the posted review comment"))' \
'(--comment-file)--comment=[Review outcome comment body. Mutually exclusive with \`--comment-file\`]:COMMENT:_default' \
'--comment-file=[Read review outcome comment body from a file. Use \`-\` for stdin]:PATH:_default' \
'*--lens=[Review lens name to include in output and issue mirror metadata]:LENS:_default' \
'--issue=[Issue number that should receive the optional activity mirror]:ISSUE_NUMBER:_default' \
'--expected-head=[Full PR head SHA that was reviewed. Required with \`--submit-review\` or \`--metadata-only\`; bound to the provider head and native review commit]:SHA:_default' \
'--thread-file=[Create or validate review threads from a JSON array of actionable findings (max 50 threads; 16 KiB body each). Requires --submit-review when posting; may be inherited by \`validate\` without posting. Omit this for summary-only reviews. Repair findings, then resolve via \`pr review-threads resolve\`]:PATH:_default' \
'--native-review-url=[Authoritative native review URL. Required with --metadata-only]:URL:_default' \
'--native-review-author=[Expected GitHub login that authored the authoritative native review. Required with --metadata-only and verified by provider read-back before the personal-identity metadata breadcrumb is posted]:LOGIN:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--mirror-issue[Mirror a compact activity note to \`--issue\` (required; omitting it returns the \`issue_required\` envelope at runtime)]' \
'--submit-review[Submit a native provider review event instead of posting an outcome comment. On GitHub this POSTs \`.../pulls/<id>/reviews\`, creating the \`#pullrequestreview-\` object; \`--decision\` maps to the review event (comments-only→COMMENT, approve→APPROVE, request-changes→REQUEST_CHANGES). The review is authored by the invoking token'\''s identity (e.g. a reviewer bot via FORGE_BOT_PROFILE). A body is required for COMMENT and REQUEST_CHANGES and optional for APPROVE. GitHub-only in v1]' \
'--recover-pending[Recover from \`github_pending_review_exists\` instead of failing\: delete the exact abandoned viewer-owned pending review this submission would have replaced, then submit. Recovery is accepted only when the PR owns exactly one viewer-authored pending review, the viewer may delete it, it is bound to \`--expected-head\`, and its body is byte-identical to the body being submitted; anything else preserves the original \`github_pending_review_exists\` error untouched. The destructive delete runs the same guards, lease, and read-back as \`pr pending-review delete --confirm-abandoned\`]' \
'--specialist-report[Validate the body as the canonical specialist Review Report shape]' \
'--metadata-only[Post only a concise personal-identity metadata breadcrumb for an already-published authoritative native review]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::id -- Numeric PR / MR id:_default' \
":: :_forge-cli__subcmd__pr__subcmd__review_commands" \
"*::: :->review" \
&& ret=0

    case $state in
    (review)
        words=($line[2] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-review-command-$line[2]:"
        case $line[2] in
            (validate)
_arguments "${_arguments_options[@]}" : \
'(--comment-file)--comment=[Review outcome comment body. Mutually exclusive with \`--comment-file\`]:COMMENT:_default' \
'--comment-file=[Read review outcome comment body from a file. Use \`-\` for stdin]:PATH:_default' \
'--thread-file=[Validate a JSON array of actionable review-thread specs]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--check-diff[Check thread path / line coordinates against the live GitHub PR diff]' \
'--specialist-report[Enforce the canonical specialist Review Report marker, fields, and table]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::id -- Numeric PR id. Required with `--check-diff`:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__review__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-review-help-command-$line[1]:"
        case $line[1] in
            (validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(comments)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(ready)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(review-threads)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__pr__subcmd__review-threads_commands" \
"*::: :->review-threads" \
&& ret=0

    case $state in
    (review-threads)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-review-threads-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(resolve)
_arguments "${_arguments_options[@]}" : \
'--thread=[Thread node id to resolve (GitHub \`PRRT_...\`)]:THREAD_ID:_default' \
'(--note-file)--note=[Optional reply body posted before resolving. Mutually exclusive with \`--note-file\`]:NOTE:_default' \
'--note-file=[Read the optional reply body from a file. Use \`-\` for stdin]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(reply)
_arguments "${_arguments_options[@]}" : \
'--thread=[Thread node id to reply to (GitHub \`PRRT_...\`)]:THREAD_ID:_default' \
'(--body-file)--body=[Reply body. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read the reply body from a file. Use \`-\` for stdin]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-review-threads-help-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(resolve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reply)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(reviews)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric pull request id:_default' \
&& ret=0
;;
(pending-review)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__pr__subcmd__pending-review_commands" \
"*::: :->pending-review" \
&& ret=0

    case $state in
    (pending-review)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-pending-review-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
'--review=[]:REVIEW_ID:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(resume-submit)
_arguments "${_arguments_options[@]}" : \
'--review=[]:REVIEW_ID:_default' \
'--review-run-id=[]:DIGEST:_default' \
'--expected-head=[]:SHA:_default' \
'--expected-commit=[]:SHA:_default' \
'--expected-snapshot=[]:DIGEST:_default' \
'--decision=[]:DECISION:((comments-only\:"Post an informational review outcome comment only"
approve\:"Record an approval outcome in the posted review comment"
request-changes\:"Record a request-changes outcome in the posted review comment"))' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(submit)
_arguments "${_arguments_options[@]}" : \
'--review=[]:REVIEW_ID:_default' \
'--expected-head=[]:SHA:_default' \
'--expected-commit=[]:SHA:_default' \
'--expected-snapshot=[]:DIGEST:_default' \
'--decision=[]:DECISION:((comments-only\:"Post an informational review outcome comment only"
approve\:"Record an approval outcome in the posted review comment"
request-changes\:"Record a request-changes outcome in the posted review comment"))' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--confirm-unmarked-submit[]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(discard)
_arguments "${_arguments_options[@]}" : \
'--review=[]:REVIEW_ID:_default' \
'--expected-head=[]:SHA:_default' \
'--expected-commit=[]:SHA:_default' \
'--expected-snapshot=[]:DIGEST:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--confirm-discard[]' \
'--confirm-inline-content-loss[]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" : \
'--review=[Pending review node id (\`PRR_...\`) returned by \`pr reviews\`]:REVIEW_ID:_default' \
'--expected-head=[Pull-request head that must still be current immediately before deletion]:SHA:_default' \
'--expected-commit=[Commit to which the pending review must still be bound]:SHA:_default' \
'(--expected-body-file)--expected-body=[Exact pending-review body expected before deletion]:EXPECTED_BODY:_default' \
'(--expected-body)--expected-body-file=[Read the exact expected pending-review body from a file. Use \`-\` for stdin]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--confirm-abandoned[Confirm that the exact guarded pending review is an abandoned draft]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric pull request id that must own the pending review:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-pending-review-help-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(resume-submit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(submit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(discard)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(review-loop)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__pr__subcmd__review-loop_commands" \
"*::: :->review-loop" \
&& ret=0

    case $state in
    (review-loop)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-review-loop-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(observe)
_arguments "${_arguments_options[@]}" : \
'--expected-head=[Exact provider head SHA whose review observation is being appended]:SHA:_default' \
'--findings-file=[Delivery-mode review-specialists merge envelope or observation array. Both shapes require a \`lifecycle_fingerprint\`; only the array accepts \`disposition\`. See this command'\''s help footer for the full schemas]:PATH:_default' \
'--expected-state=[Exact current provider-visible state-chain tip; omit only for genesis]:DIGEST:_default' \
'(--body-file)--body=[Human-readable delivery outcome to post in the same comment as this ledger record. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read the delivery outcome body from PATH (\`-\` reads stdin)]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'(--expected-state)--auto-state[Read the current chain tip and compare-and-swap against it, instead of naming it with \`--expected-state\`. See this command'\''s help footer for what this does NOT detect]' \
'--preflight[Run the full \`--dry-run\` verdict sweep before this live append and stop with \`review_preflight_failed\` if any rule fails]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(extend)
_arguments "${_arguments_options[@]}" : \
'--expected-head=[Exact provider head SHA whose active hard stop is being extended]:SHA:_default' \
'--expected-state=[]:DIGEST:_default' \
'--stop-code=[]:CODE:_default' \
'--budget-field=[]:FIELD:(max_repair_rounds max_no_progress_rounds max_auto_reopens_per_fingerprint)' \
'--increment=[]:INCREMENT:_default' \
'--proposal-digest=[]:DIGEST:_default' \
'--approval-comment=[Numeric GitHub issue-comment id carrying the exact approval marker]:COMMENT_ID:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id:_default' \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" : \
'--findings-file=[Delivery-mode review-specialists merge envelope or observation array]:PATH:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-review-loop-help-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(observe)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(extend)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(tasks)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(merge)
_arguments "${_arguments_options[@]}" : \
'--expected-head=[Compare-and-swap head verified by the caller before merge]:SHA:_default' \
'--expected-base=[Exact target / base branch the provider PR/MR must still use]:BRANCH:_default' \
'--method=[Merge method override. When omitted, falls back to \`.forge-cli.toml \[merge\].method\` then the spec default \`squash\`]:METHOD:(squash merge rebase)' \
'--allow-unresolved-threads-reason=[Required when \`--allow-unresolved-threads\` is set. Non-empty free-form text describing why the unresolved threads are safe to merge past; the reason is recorded in the merge envelope payload]:TEXT:_default' \
'--review-convergence=[Override \`\[review_convergence\].require\`. Passing the flag without a value enables it; use \`--review-convergence=false\` to disable a repo or user-global opt-in for this invocation]::REVIEW_CONVERGENCE:(true false)' \
'--allow-no-checks-reason=[Required when \`--allow-no-checks\` is set. Non-empty free-form text describing why merging an unchecked head is safe — normally that the repository configures no checks at all; the reason is recorded in the merge envelope payload]:TEXT:_default' \
'--allow-unchecked-tasks-reason=[Required when \`--allow-unchecked-tasks\` is set. Non-empty free-form text describing why the unchecked items are safe to merge past; the reason is recorded in the merge envelope payload]:TEXT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--keep-branch[Skip the post-merge branch deletion (\`--delete-branch\` on gh, \`--remove-source-branch\` on glab). Mutually exclusive with the implicit-true default; flagging both is \`keep_branch_conflict\`]' \
'--allow-non-default-base[Allow merges where the PR'\''s base is not the repo'\''s default branch. Without this flag, mismatched bases trigger \`default_branch_protected\`]' \
'--allow-unresolved-threads[Merge despite unresolved review threads. Without this flag, any non-outdated unresolved thread (bot or human) triggers \`unresolved_review_threads\`. Requires \`--allow-unresolved-threads-reason\`]' \
'--allow-no-checks[Merge a head for which no required checks are registered. Without this flag an empty required-check snapshot triggers \`checks_not_registered\`, because "all required checks passed" is vacuously true over an empty set and so proves nothing about the head. Requires \`--allow-no-checks-reason\`]' \
'--allow-unchecked-tasks[Merge despite unchecked task-list items in the PR/MR description. Without this flag, any unchecked \`- \[ \]\` item triggers \`unchecked_task_items\`. Requires \`--allow-unchecked-tasks-reason\`]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric PR / MR id:_default' \
&& ret=0
;;
(close)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric id:_default' \
&& ret=0
;;
(checks)
_arguments "${_arguments_options[@]}" : \
'--required-only=[Restrict the gating decision to required checks (default \`true\`). Non-required checks are always reported in \`data.checks\` regardless]::REQUIRED_ONLY:(true false)' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric id or branch name (GitHub accepts both natively; GitLab resolves numeric ids via `mr view` to fetch the source branch):_default' \
&& ret=0
;;
(wait-checks)
_arguments "${_arguments_options[@]}" : \
'--timeout=[Total budget before declaring \`checks_timeout\` (default \`30m\`)]:TIMEOUT:_default' \
'--interval=[Pause between polls (default \`20s\`)]:INTERVAL:_default' \
'--required-only=[Restrict the gating decision to required checks (default \`true\`)]::REQUIRED_ONLY:(true false)' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--allow-no-checks[Treat a head with no registered checks as complete instead of waiting out the timeout. Without it, an empty check set is not terminal and the wait expires as \`checks_not_registered\`]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric id or branch name:_default' \
&& ret=0
;;
(deliver)
_arguments "${_arguments_options[@]}" : \
'--kind=[PR / MR kind (selects branch-prefix rule). Required]:KIND:(feature bug chore docs ci refactor)' \
'--title=[PR / MR title (required)]:TITLE:_default' \
'(--body-file)--body=[PR / MR body / description text. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read PR / MR body from a file. Use \`-\` to read from stdin]:PATH:_default' \
'--head=[Source branch (defaults to the current branch)]:HEAD:_default' \
'--base=[Target / base branch (defaults to the repo'\''s default branch)]:BASE:_default' \
'--method=[Merge method (default\: squash)]:METHOD:(squash merge rebase)' \
'*--reviewer=[Add a reviewer (repeatable)]:USER:_default' \
'*--label=[Add a label to the created PR / MR (repeatable)]:LABEL:_default' \
'--label-catalog=[Validate labels against this catalog when \`--strict-labels\` is set]:PATH:_default' \
'--test-first-evidence=[Directory holding a \`test-first-evidence\` record, forwarded to the create step and verified by the test-first gate when \`\[test_first\].require\` resolves true for a feature/bug PR. Requires a complete v2 record with a testable classification and a bound baseline/delivery subject matching the current checkout]:DIR:_default' \
'--timeout=[Cumulative CI-wait budget before declaring \`checks_timeout\` (default \`30m\`)]:TIMEOUT:_default' \
'--allow-unresolved-threads-reason=[Required when \`--allow-unresolved-threads\` is set. Non-empty free-form text describing why the unresolved threads are safe to merge past; the reason is recorded in the merge-step payload]:TEXT:_default' \
'--review-convergence=[Override \`\[review_convergence\].require\` for the merge phase. Passing the flag without a value enables it; \`--review-convergence=false\` disables a configured opt-in for this invocation]::REVIEW_CONVERGENCE:(true false)' \
'--allow-unchecked-tasks-reason=[Required when \`--allow-unchecked-tasks\` is set. Non-empty free-form text describing why the unchecked items are safe to merge past; the reason is recorded in the merge-step payload]:TEXT:_default' \
'--allow-no-checks-reason=[Required when \`--allow-no-checks\` is set. Non-empty free-form text describing why delivering an unchecked head is safe; the reason is recorded in the merge-step payload]:TEXT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--strict-labels[Fail when selected labels are missing, not applicable, or conflict]' \
'--no-merge[Stop after \`pr.wait-checks\` — do not promote to ready or merge]' \
'--no-issue-closeout[Skip the post-merge linked-issue closeout. By default, after a successful merge the macro deterministically closes any still-open issue referenced by a \`Closes/Fixes #N\` closing keyword (GitHub surfaces these via \`closingIssuesReferences\`), so delivery does not depend on GitHub'\''s asynchronous auto-close latency. Plan-tracking PRs use non-closing \`Refs #N\` (empty \`closingIssuesReferences\`) and are unaffected either way]' \
'--allow-non-default-base[Allow merges where the PR'\''s base is not the repo'\''s default branch]' \
'--allow-unresolved-threads[Merge despite unresolved review threads. Without this flag, any non-outdated unresolved thread (bot or human) triggers \`unresolved_review_threads\` at the merge step. Requires \`--allow-unresolved-threads-reason\`]' \
'--allow-unchecked-tasks[Merge despite unchecked task-list items in the PR/MR description. Without this flag, any unchecked \`- \[ \]\` item triggers \`unchecked_task_items\` at the merge step. Requires \`--allow-unchecked-tasks-reason\`]' \
'--allow-no-checks[Deliver a head for which no checks are registered. Without this flag, delivery waits out its \`--timeout\` for checks to appear and then fails with \`checks_not_registered\` rather than treating an empty check set as a pass. Requires \`--allow-no-checks-reason\`]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-help-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(comment)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(review)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__help__subcmd__review_commands" \
"*::: :->review" \
&& ret=0

    case $state in
    (review)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-help-review-command-$line[1]:"
        case $line[1] in
            (validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(comments)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ready)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(review-threads)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads_commands" \
"*::: :->review-threads" \
&& ret=0

    case $state in
    (review-threads)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-help-review-threads-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(resolve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reply)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(reviews)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pending-review)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review_commands" \
"*::: :->pending-review" \
&& ret=0

    case $state in
    (pending-review)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-help-pending-review-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(resume-submit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(submit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(discard)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(review-loop)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop_commands" \
"*::: :->review-loop" \
&& ret=0

    case $state in
    (review-loop)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-pr-help-review-loop-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(observe)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(extend)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(tasks)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(merge)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(close)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(checks)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(wait-checks)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(deliver)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(issue)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__issue_commands" \
"*::: :->issue" \
&& ret=0

    case $state in
    (issue)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-issue-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
'--title=[Issue title (≤70 chars per \`title_length\` rule)]:TITLE:_default' \
'(--body-file)--body=[Issue body (inline). Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read body from a file. Use \`-\` for stdin]:BODY_FILE:_default' \
'*--label=[Add a label. Repeat to apply multiple labels]:NAME:_default' \
'*--assignee=[Assign a user. Repeat to assign multiple]:LOGIN:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--with-comments[Also fetch the issue'\''s comment stream and embed it under \`comments\` in the envelope payload. Adds one GitLab API call; for GitHub the comments are pulled in the same \`gh issue view --json\` invocation]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric issue id:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--state=[Filter by state (default\: open)]:STATE:(open closed all)' \
'*--label=[Filter by label. Repeatable. GitHub joins labels into a comma list (issue must have all labels); GitLab passes a repeated --label]:NAME:_default' \
'--author=[Filter by author handle]:AUTHOR:_default' \
'--assignee=[Filter by assignee handle]:ASSIGNEE:_default' \
'--limit=[Cap the number of returned issues (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
'--title=[New title (re-validated against \`title_length\`)]:TITLE:_default' \
'(--body-file)--body=[New body. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read body from a file. Use \`-\` for stdin]:BODY_FILE:_default' \
'*--add-label=[Add a label. Repeat to add multiple. Accepts \`--label\` as a shorthand matching \`issue create --label\`]:NAME:_default' \
'*--remove-label=[Remove a label. Repeat to remove multiple]:NAME:_default' \
'*--add-assignee=[Add an assignee. Repeat to assign multiple]:LOGIN:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric issue id:_default' \
&& ret=0
;;
(comment)
_arguments "${_arguments_options[@]}" : \
'(--body-file)--body=[Comment body. Mutually exclusive with \`--body-file\`]:BODY:_default' \
'--body-file=[Read body from a file. Use \`-\` for stdin]:BODY_FILE:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric issue id:_default' \
&& ret=0
;;
(close)
_arguments "${_arguments_options[@]}" : \
'--reason=[State reason recorded on close (GitHub only). \`completed\` marks the issue done; \`not planned\` marks it abandoned. GitLab / Local have no equivalent and silently ignore this flag]:REASON:(completed not planned)' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric issue id:_default' \
&& ret=0
;;
(reopen)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- Numeric id:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__issue__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-issue-help-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(comment)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(close)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reopen)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(activity)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__activity_commands" \
"*::: :->activity" \
&& ret=0

    case $state in
    (activity)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-activity-command-$line[1]:"
        case $line[1] in
            (commits)
_arguments "${_arguments_options[@]}" : \
'--user=[GitHub login to inspect. Use @me for the authenticated account]:LOGIN:_default' \
'--since=[Only include commits authored at or after this date/datetime]:DATE_OR_DATETIME:_default' \
'--limit=[Cap the number of returned commits (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(events)
_arguments "${_arguments_options[@]}" : \
'--user=[GitHub login to inspect. Use @me for the authenticated account]:LOGIN:_default' \
'--limit=[Cap the number of returned events (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--public-only[Use the public-events endpoint even for @me]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(feed)
_arguments "${_arguments_options[@]}" : \
'--since=[Only include activity at or after this date/datetime]:DATE_OR_DATETIME:_default' \
'--limit=[Cap the number of returned activity records (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(summary)
_arguments "${_arguments_options[@]}" : \
'--user=[GitHub login to inspect. Use @me for the authenticated account]:LOGIN:_default' \
'--since=[Only count contributions at or after this date/datetime]:DATE_OR_DATETIME:_default' \
'--limit=[Maximum repositories to include in the summary (default\: 25)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__activity__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-activity-help-command-$line[1]:"
        case $line[1] in
            (commits)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(events)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(feed)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(summary)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(label)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__label_commands" \
"*::: :->label" \
&& ret=0

    case $state in
    (label)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-label-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
'--limit=[Maximum labels to fetch from the provider]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(audit)
_arguments "${_arguments_options[@]}" : \
'--catalog=[Path to the shared forge label catalog]:PATH:_default' \
'--limit=[Maximum labels to fetch from the provider]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(ensure)
_arguments "${_arguments_options[@]}" : \
'--catalog=[Path to the shared forge label catalog]:PATH:_default' \
'--limit=[Maximum labels to fetch from the provider]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--update-existing[Update color / description drift on existing labels]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__label__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-label-help-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(audit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ensure)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(inbox)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__inbox_commands" \
"*::: :->inbox" \
&& ret=0

    case $state in
    (inbox)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-inbox-command-$line[1]:"
        case $line[1] in
            (status)
_arguments "${_arguments_options[@]}" : \
'--gitlab-host=[GitLab host for inbox API calls. Defaults from FORGE_CLI_INBOX_GITLAB_HOST, GitLab remote inference, then gitlab.com]:HOST:_default' \
'*--kind=[Restrict inbox reasons/kinds. Repeatable. Defaults to review, assigned, todo, and authored; \`involved\` is opt-in because it can be broad on GitHub]:KINDS:(review assigned todo authored involved)' \
'--item-type=[Restrict inbox by item type (PR/MR vs issue). \`--kind\` filters reasons (review/assigned/todo/authored/involved); \`--item-type\` filters result classes so PR-only and issue-only modes skip irrelevant provider queries. Defaults to all]:ITEM_TYPE:((all\:"Include PRs/MRs, issues, and classifiable todos (default)"
pr\:"Include only pull requests / merge requests (and todos that target them)"
issue\:"Include only issues (and todos that target them)"))' \
'--limit=[Per-provider, per-query-family bounded result limit (default\: 30)]:LIMIT:_default' \
'--gitlab-vpn=[GitLab VPN readiness policy for inbox calls]:GITLAB_VPN:((off\:"Do not run a GitLab VPN readiness probe (default)"
optional\:"Run the probe when configured, but attempt GitLab even when it fails"
required\:"Require readiness before any GitLab backend call"))' \
'--gitlab-vpn-check=[GitLab VPN readiness check\: \`tcp\:<host>\:<port>\`, \`cmd\:<program>\`, or \`openvpn\`]:CHECK:_default' \
'--gitlab-vpn-check-timeout=[GitLab VPN readiness check timeout (default\: 2s)]:GITLAB_VPN_CHECK_TIMEOUT:_default' \
'--gitlab-openvpn-profile=[Local OpenVPN profile path for probe diagnostics. Redacted from output]:PATH:_files' \
'--provider-timeout=[Per-backend-call timeout for GitLab inbox calls (default\: 20s; 0 disables)]:PROVIDER_TIMEOUT:_default' \
'--cache-max-age=[Maximum age for opt-in cached fallback items (default\: 30m)]:CACHE_MAX_AGE:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--strict-providers[Fail when any selected inbox provider fails]' \
'--cache-fallback[Include recent cached items when a selected provider is unavailable or times out]' \
'--no-cache[Disable inbox cache reads and writes]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--gitlab-host=[GitLab host for inbox API calls. Defaults from FORGE_CLI_INBOX_GITLAB_HOST, GitLab remote inference, then gitlab.com]:HOST:_default' \
'*--kind=[Restrict inbox reasons/kinds. Repeatable. Defaults to review, assigned, todo, and authored; \`involved\` is opt-in because it can be broad on GitHub]:KINDS:(review assigned todo authored involved)' \
'--item-type=[Restrict inbox by item type (PR/MR vs issue). \`--kind\` filters reasons (review/assigned/todo/authored/involved); \`--item-type\` filters result classes so PR-only and issue-only modes skip irrelevant provider queries. Defaults to all]:ITEM_TYPE:((all\:"Include PRs/MRs, issues, and classifiable todos (default)"
pr\:"Include only pull requests / merge requests (and todos that target them)"
issue\:"Include only issues (and todos that target them)"))' \
'--limit=[Per-provider, per-query-family bounded result limit (default\: 30)]:LIMIT:_default' \
'--gitlab-vpn=[GitLab VPN readiness policy for inbox calls]:GITLAB_VPN:((off\:"Do not run a GitLab VPN readiness probe (default)"
optional\:"Run the probe when configured, but attempt GitLab even when it fails"
required\:"Require readiness before any GitLab backend call"))' \
'--gitlab-vpn-check=[GitLab VPN readiness check\: \`tcp\:<host>\:<port>\`, \`cmd\:<program>\`, or \`openvpn\`]:CHECK:_default' \
'--gitlab-vpn-check-timeout=[GitLab VPN readiness check timeout (default\: 2s)]:GITLAB_VPN_CHECK_TIMEOUT:_default' \
'--gitlab-openvpn-profile=[Local OpenVPN profile path for probe diagnostics. Redacted from output]:PATH:_files' \
'--provider-timeout=[Per-backend-call timeout for GitLab inbox calls (default\: 20s; 0 disables)]:PROVIDER_TIMEOUT:_default' \
'--cache-max-age=[Maximum age for opt-in cached fallback items (default\: 30m)]:CACHE_MAX_AGE:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--strict-providers[Fail when any selected inbox provider fails]' \
'--cache-fallback[Include recent cached items when a selected provider is unavailable or times out]' \
'--no-cache[Disable inbox cache reads and writes]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(next)
_arguments "${_arguments_options[@]}" : \
'--gitlab-host=[GitLab host for inbox API calls. Defaults from FORGE_CLI_INBOX_GITLAB_HOST, GitLab remote inference, then gitlab.com]:HOST:_default' \
'*--kind=[Restrict inbox reasons/kinds. Repeatable. Defaults to review, assigned, todo, and authored; \`involved\` is opt-in]:KINDS:(review assigned todo authored involved)' \
'--item-type=[Restrict inbox by item type (PR/MR vs issue). See \`inbox list --help\` for the distinction from \`--kind\`. Defaults to all]:ITEM_TYPE:((all\:"Include PRs/MRs, issues, and classifiable todos (default)"
pr\:"Include only pull requests / merge requests (and todos that target them)"
issue\:"Include only issues (and todos that target them)"))' \
'--limit=[Number of ranked items to return (default\: 5). Provider queries remain bounded at at least 30 candidates so ranking has enough input]:LIMIT:_default' \
'--gitlab-vpn=[GitLab VPN readiness policy for inbox calls]:GITLAB_VPN:((off\:"Do not run a GitLab VPN readiness probe (default)"
optional\:"Run the probe when configured, but attempt GitLab even when it fails"
required\:"Require readiness before any GitLab backend call"))' \
'--gitlab-vpn-check=[GitLab VPN readiness check\: \`tcp\:<host>\:<port>\`, \`cmd\:<program>\`, or \`openvpn\`]:CHECK:_default' \
'--gitlab-vpn-check-timeout=[GitLab VPN readiness check timeout (default\: 2s)]:GITLAB_VPN_CHECK_TIMEOUT:_default' \
'--gitlab-openvpn-profile=[Local OpenVPN profile path for probe diagnostics. Redacted from output]:PATH:_files' \
'--provider-timeout=[Per-backend-call timeout for GitLab inbox calls (default\: 20s; 0 disables)]:PROVIDER_TIMEOUT:_default' \
'--cache-max-age=[Maximum age for opt-in cached fallback items (default\: 30m)]:CACHE_MAX_AGE:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--strict-providers[Fail when any selected inbox provider fails]' \
'--cache-fallback[Include recent cached items when a selected provider is unavailable or times out]' \
'--no-cache[Disable inbox cache reads and writes]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__inbox__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-inbox-help-command-$line[1]:"
        case $line[1] in
            (status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(next)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(search)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__search_commands" \
"*::: :->search" \
&& ret=0

    case $state in
    (search)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-search-command-$line[1]:"
        case $line[1] in
            (issues)
_arguments "${_arguments_options[@]}" : \
'*--match=[Restrict matching to these fields (comma-separated or repeatable). Defaults to title, body, and comments]:FIELD:(title body comments)' \
'--limit=[Cap the number of returned results (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':query -- Free-text query passed to the provider'\''s search primitive:_default' \
&& ret=0
;;
(prs)
_arguments "${_arguments_options[@]}" : \
'*--match=[Restrict matching to these fields (comma-separated or repeatable). Defaults to title, body, and comments]:FIELD:(title body comments)' \
'--limit=[Cap the number of returned results (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':query -- Free-text query passed to the provider'\''s search primitive:_default' \
&& ret=0
;;
(refs-to)
_arguments "${_arguments_options[@]}" : \
'--limit=[Cap the number of cross-reference events scanned (default\: 30)]:LIMIT:_default' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':reference -- Target issue / PR to find references to. Accepts a GitHub URL, `owner/name#number`, or `#number` / `number` (repo from context):_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__search__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-search-help-command-$line[1]:"
        case $line[1] in
            (issues)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(prs)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(refs-to)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(repo)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__repo_commands" \
"*::: :->repo" \
&& ret=0

    case $state in
    (repo)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-repo-command-$line[1]:"
        case $line[1] in
            (view)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(bootstrap)
_arguments "${_arguments_options[@]}" : \
'--owner-kind=[Whether the repository owner is the authenticated user or an organization]:OWNER_KIND:(user org)' \
'--default-branch=[Exact branch name to create and establish as the remote default]:BRANCH:_default' \
'*--file=[Regular file to copy into the repository root. Repeat for multiple files]:PATH:_files' \
'--message=[Semantic Commit message for the signed zero-parent root commit]:TEXT:_default' \
'--reason-file=[UTF-8 file describing the user'\''s explicit bootstrap authorization]:PATH:_files' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--resume[Resume an existing durable bootstrap receipt after exact read-back]' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(push-default)
_arguments "${_arguments_options[@]}" : \
'--head=[Commit-ish to deliver. It must resolve to the currently checked-out HEAD]:HEAD:_default' \
'--expected-base=[Exact remote default-branch SHA observed before authoring the commit]:SHA:_default' \
'--reason-file=[UTF-8 file describing the user'\''s explicit direct-main authorization]:PATH:_files' \
'--default-branch-receipt=[Governed default-branch receipt authorizing checked-default adoption]:PATH:_files' \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__repo__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-repo-help-command-$line[1]:"
        case $line[1] in
            (view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(bootstrap)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(push-default)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(auth)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_forge-cli__subcmd__auth_commands" \
"*::: :->auth" \
&& ret=0

    case $state in
    (auth)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-auth-command-$line[1]:"
        case $line[1] in
            (status)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__auth__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-auth-help-command-$line[1]:"
        case $line[1] in
            (status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(operation-effect)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::command:_default' \
&& ret=0
;;
(completion)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format (defaults to text)]:FORMAT:((text\:"Human-readable text output (default)"
json\:"Single-record JSON envelope (snake_case)"))' \
'--remote=[Git remote whose URL feeds provider detection (default\: \`origin\`)]:REMOTE:_default' \
'--provider=[Override auto-detected provider]:PROVIDER:(github gitlab local)' \
'--host=[Override the forge authority (\`hostname\` or \`hostname\:port\`). With \`--provider\`, a syntactically valid custom authority is accepted unless it is positively identified as the other provider. Without \`--provider\`, the authority must identify a supported provider]:HOST:_default' \
'--repo=[Override the remote-derived repository path. Accepted shapes\: GitHub \`owner/name\`; GitLab \`group\[/subgroup...\]/project\`; Local \`local\:<slug>\` or \`<slug>\`]:REPOSITORY:_default' \
'--store-root=[Root directory of the file-backed store used by \`--provider local\`. Overrides the \`FORGE_CLI_LOCAL_STORE\` env var]:path:_files' \
'--dry-run[Validate and describe the planned operation without applying its mutation. Read-only preflight calls may still run]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':shell -- Target shell:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-command-$line[1]:"
        case $line[1] in
            (provider)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__provider_commands" \
"*::: :->provider" \
&& ret=0

    case $state in
    (provider)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-provider-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(pr)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__pr_commands" \
"*::: :->pr" \
&& ret=0

    case $state in
    (pr)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-pr-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(comment)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(review)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__pr__subcmd__review_commands" \
"*::: :->review" \
&& ret=0

    case $state in
    (review)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-pr-review-command-$line[1]:"
        case $line[1] in
            (validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(comments)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ready)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(review-threads)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads_commands" \
"*::: :->review-threads" \
&& ret=0

    case $state in
    (review-threads)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-pr-review-threads-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(resolve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reply)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(reviews)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pending-review)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review_commands" \
"*::: :->pending-review" \
&& ret=0

    case $state in
    (pending-review)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-pr-pending-review-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(resume-submit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(submit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(discard)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(review-loop)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop_commands" \
"*::: :->review-loop" \
&& ret=0

    case $state in
    (review-loop)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-pr-review-loop-command-$line[1]:"
        case $line[1] in
            (inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(observe)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(extend)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(tasks)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(merge)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(close)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(checks)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(wait-checks)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(deliver)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(issue)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__issue_commands" \
"*::: :->issue" \
&& ret=0

    case $state in
    (issue)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-issue-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(comment)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(close)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reopen)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(activity)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__activity_commands" \
"*::: :->activity" \
&& ret=0

    case $state in
    (activity)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-activity-command-$line[1]:"
        case $line[1] in
            (commits)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(events)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(feed)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(summary)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(label)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__label_commands" \
"*::: :->label" \
&& ret=0

    case $state in
    (label)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-label-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(audit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ensure)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(inbox)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__inbox_commands" \
"*::: :->inbox" \
&& ret=0

    case $state in
    (inbox)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-inbox-command-$line[1]:"
        case $line[1] in
            (status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(next)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(search)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__search_commands" \
"*::: :->search" \
&& ret=0

    case $state in
    (search)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-search-command-$line[1]:"
        case $line[1] in
            (issues)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(prs)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(refs-to)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(repo)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__repo_commands" \
"*::: :->repo" \
&& ret=0

    case $state in
    (repo)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-repo-command-$line[1]:"
        case $line[1] in
            (view)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(bootstrap)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(push-default)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(auth)
_arguments "${_arguments_options[@]}" : \
":: :_forge-cli__subcmd__help__subcmd__auth_commands" \
"*::: :->auth" \
&& ret=0

    case $state in
    (auth)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:forge-cli-help-auth-command-$line[1]:"
        case $line[1] in
            (status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(operation-effect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completion)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_forge-cli_commands] )) ||
_forge-cli_commands() {
    local commands; commands=(
'provider:Manage named forge provider records' \
'pr:Pull / merge request lifecycle' \
'issue:Issue lifecycle' \
'activity:Personal activity across forge repositories' \
'label:Repository label catalog audit and ensure operations' \
'inbox:Personal cross-repo work inbox' \
'search:Full-text / reverse-reference search over issues and PRs' \
'repo:Repository helpers' \
'auth:Backend authentication helpers' \
'operation-effect:Describe the effect of one exact typed forge-cli invocation' \
'completion:Emit shell-completion scripts' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity_commands] )) ||
_forge-cli__subcmd__activity_commands() {
    local commands; commands=(
'commits:Search recent GitHub commits authored by a user' \
'events:List recent GitHub user activity events' \
'feed:List recent repository/project activity' \
'summary:Summarize GitHub commit contributions by repository' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli activity commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__commits_commands] )) ||
_forge-cli__subcmd__activity__subcmd__commits_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity commits commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__events_commands] )) ||
_forge-cli__subcmd__activity__subcmd__events_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity events commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__feed_commands] )) ||
_forge-cli__subcmd__activity__subcmd__feed_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity feed commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__help_commands] )) ||
_forge-cli__subcmd__activity__subcmd__help_commands() {
    local commands; commands=(
'commits:Search recent GitHub commits authored by a user' \
'events:List recent GitHub user activity events' \
'feed:List recent repository/project activity' \
'summary:Summarize GitHub commit contributions by repository' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli activity help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__help__subcmd__commits_commands] )) ||
_forge-cli__subcmd__activity__subcmd__help__subcmd__commits_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity help commits commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__help__subcmd__events_commands] )) ||
_forge-cli__subcmd__activity__subcmd__help__subcmd__events_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity help events commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__help__subcmd__feed_commands] )) ||
_forge-cli__subcmd__activity__subcmd__help__subcmd__feed_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity help feed commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__activity__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__help__subcmd__summary_commands] )) ||
_forge-cli__subcmd__activity__subcmd__help__subcmd__summary_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity help summary commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__activity__subcmd__summary_commands] )) ||
_forge-cli__subcmd__activity__subcmd__summary_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli activity summary commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__auth_commands] )) ||
_forge-cli__subcmd__auth_commands() {
    local commands; commands=(
'status:Verify backend auth (gh / glab)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli auth commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__auth__subcmd__help_commands] )) ||
_forge-cli__subcmd__auth__subcmd__help_commands() {
    local commands; commands=(
'status:Verify backend auth (gh / glab)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli auth help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__auth__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__auth__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli auth help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__auth__subcmd__help__subcmd__status_commands] )) ||
_forge-cli__subcmd__auth__subcmd__help__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli auth help status commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__auth__subcmd__status_commands] )) ||
_forge-cli__subcmd__auth__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli auth status commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__completion_commands] )) ||
_forge-cli__subcmd__completion_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli completion commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help_commands] )) ||
_forge-cli__subcmd__help_commands() {
    local commands; commands=(
'provider:Manage named forge provider records' \
'pr:Pull / merge request lifecycle' \
'issue:Issue lifecycle' \
'activity:Personal activity across forge repositories' \
'label:Repository label catalog audit and ensure operations' \
'inbox:Personal cross-repo work inbox' \
'search:Full-text / reverse-reference search over issues and PRs' \
'repo:Repository helpers' \
'auth:Backend authentication helpers' \
'operation-effect:Describe the effect of one exact typed forge-cli invocation' \
'completion:Emit shell-completion scripts' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__activity_commands] )) ||
_forge-cli__subcmd__help__subcmd__activity_commands() {
    local commands; commands=(
'commits:Search recent GitHub commits authored by a user' \
'events:List recent GitHub user activity events' \
'feed:List recent repository/project activity' \
'summary:Summarize GitHub commit contributions by repository' \
    )
    _describe -t commands 'forge-cli help activity commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__activity__subcmd__commits_commands] )) ||
_forge-cli__subcmd__help__subcmd__activity__subcmd__commits_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help activity commits commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__activity__subcmd__events_commands] )) ||
_forge-cli__subcmd__help__subcmd__activity__subcmd__events_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help activity events commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__activity__subcmd__feed_commands] )) ||
_forge-cli__subcmd__help__subcmd__activity__subcmd__feed_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help activity feed commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__activity__subcmd__summary_commands] )) ||
_forge-cli__subcmd__help__subcmd__activity__subcmd__summary_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help activity summary commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__auth_commands] )) ||
_forge-cli__subcmd__help__subcmd__auth_commands() {
    local commands; commands=(
'status:Verify backend auth (gh / glab)' \
    )
    _describe -t commands 'forge-cli help auth commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__auth__subcmd__status_commands] )) ||
_forge-cli__subcmd__help__subcmd__auth__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help auth status commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__completion_commands] )) ||
_forge-cli__subcmd__help__subcmd__completion_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help completion commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__inbox_commands] )) ||
_forge-cli__subcmd__help__subcmd__inbox_commands() {
    local commands; commands=(
'status:Summarize bounded personal work counts' \
'list:List normalized inbox items' \
'next:Return ranked next-action candidates' \
    )
    _describe -t commands 'forge-cli help inbox commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__inbox__subcmd__list_commands] )) ||
_forge-cli__subcmd__help__subcmd__inbox__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help inbox list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__inbox__subcmd__next_commands] )) ||
_forge-cli__subcmd__help__subcmd__inbox__subcmd__next_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help inbox next commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__inbox__subcmd__status_commands] )) ||
_forge-cli__subcmd__help__subcmd__inbox__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help inbox status commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue_commands() {
    local commands; commands=(
'create:Open a new issue' \
'view:Fetch a single issue' \
'list:List issues filtered by state / labels / author / assignee' \
'edit:Mutate an issue' \
'comment:Append a comment to an issue' \
'close:Close an issue' \
'reopen:Reopen a closed issue' \
    )
    _describe -t commands 'forge-cli help issue commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__close_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__close_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue close commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__comment_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__comment_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue comment commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__create_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue create commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__edit_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue edit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__list_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__reopen_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__reopen_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue reopen commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__issue__subcmd__view_commands] )) ||
_forge-cli__subcmd__help__subcmd__issue__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help issue view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__label_commands] )) ||
_forge-cli__subcmd__help__subcmd__label_commands() {
    local commands; commands=(
'list:List repository labels through the selected provider backend' \
'audit:Compare repository labels against a machine-readable catalog' \
'ensure:Create missing labels and optionally update color / description drift' \
    )
    _describe -t commands 'forge-cli help label commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__label__subcmd__audit_commands] )) ||
_forge-cli__subcmd__help__subcmd__label__subcmd__audit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help label audit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__label__subcmd__ensure_commands] )) ||
_forge-cli__subcmd__help__subcmd__label__subcmd__ensure_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help label ensure commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__label__subcmd__list_commands] )) ||
_forge-cli__subcmd__help__subcmd__label__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help label list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__operation-effect_commands] )) ||
_forge-cli__subcmd__help__subcmd__operation-effect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help operation-effect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr_commands() {
    local commands; commands=(
'create:Open a draft pull / merge request from the current branch' \
'view:Fetch a single PR / MR with normalised fields' \
'list:List PRs / MRs' \
'edit:Mutate PR / MR title, body, base, labels, reviewers' \
'comment:Append a comment to a PR / MR' \
'review:Post a review outcome comment and optionally mirror it to an issue' \
'comments:List the issue-style comment stream attached to a PR / MR' \
'ready:Promote a draft PR / MR to ready-for-review' \
'review-threads:List review threads attached to a PR / MR with their resolved state' \
'reviews:List native review summaries, classified against the current head' \
'pending-review:Recover an authenticated actor'\''s pending native review' \
'review-loop:Inspect and advance the durable review-loop ledger' \
'tasks:List GFM task-list items in the PR / MR description with their state' \
'merge:Merge a ready PR / MR' \
'close:Close a PR / MR without merging' \
'checks:One-shot snapshot of PR / MR check state' \
'wait-checks:Block until every required check reaches a terminal state' \
'deliver:End-to-end "open draft (or adopt the branch'\''s open PR) → CI green → ready → merge" macro' \
    )
    _describe -t commands 'forge-cli help pr commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__checks_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__checks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr checks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__close_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__close_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr close commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__comment_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__comment_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr comment commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__comments_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__comments_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr comments commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__create_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr create commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__deliver_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__deliver_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr deliver commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__edit_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr edit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__list_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__merge_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__merge_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr merge commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review_commands() {
    local commands; commands=(
'inspect:Read one complete pending-review snapshot and immutable digest' \
'resume-submit:Resume and submit an exact receipt-bound review transaction' \
'submit:Guardedly submit an unmarked pending review' \
'discard:Destructively discard one exact pending review' \
'delete:Verify and delete one exact pending review node' \
    )
    _describe -t commands 'forge-cli help pr pending-review commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__delete_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__delete_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr pending-review delete commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__discard_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__discard_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr pending-review discard commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr pending-review inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__resume-submit_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__resume-submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr pending-review resume-submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__submit_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__pending-review__subcmd__submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr pending-review submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__ready_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__ready_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr ready commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review_commands() {
    local commands; commands=(
'validate:Validate review summary and thread specs before posting' \
    )
    _describe -t commands 'forge-cli help pr review commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review__subcmd__validate_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop_commands() {
    local commands; commands=(
'inspect:Inspect the validated ledger tip and current round state' \
'observe:Observe one reviewed head and append the resulting state transition' \
'extend:Consume an exact provider-visible approval to extend one budget' \
'validate:Validate a findings payload offline, before it reaches the provider' \
    )
    _describe -t commands 'forge-cli help pr review-loop commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__extend_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__extend_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-loop extend commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-loop inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__observe_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__observe_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-loop observe commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__validate_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-loop__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-loop validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads_commands() {
    local commands; commands=(
'list:List review threads attached to a PR / MR with their resolved state' \
'resolve:Resolve a review thread, optionally posting a reply first (GitHub)' \
'reply:Reply to a review thread without resolving it (GitHub)' \
    )
    _describe -t commands 'forge-cli help pr review-threads commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads__subcmd__list_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-threads list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads__subcmd__reply_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads__subcmd__reply_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-threads reply commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads__subcmd__resolve_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__review-threads__subcmd__resolve_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr review-threads resolve commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__reviews_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__reviews_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr reviews commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__tasks_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__tasks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr tasks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__view_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__pr__subcmd__wait-checks_commands] )) ||
_forge-cli__subcmd__help__subcmd__pr__subcmd__wait-checks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help pr wait-checks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__provider_commands] )) ||
_forge-cli__subcmd__help__subcmd__provider_commands() {
    local commands; commands=(
'add:Add a named provider record without storing its token value' \
'list:List named provider records' \
'view:View one named provider record' \
    )
    _describe -t commands 'forge-cli help provider commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__provider__subcmd__add_commands] )) ||
_forge-cli__subcmd__help__subcmd__provider__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help provider add commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__provider__subcmd__list_commands] )) ||
_forge-cli__subcmd__help__subcmd__provider__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help provider list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__provider__subcmd__view_commands] )) ||
_forge-cli__subcmd__help__subcmd__provider__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help provider view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__repo_commands] )) ||
_forge-cli__subcmd__help__subcmd__repo_commands() {
    local commands; commands=(
'view:Resolve the repo slug, default branch, and supported merge methods' \
'bootstrap:Create and initialize one new private Forgejo repository under a durable receipt' \
'push-default:Deliver one signed commit to the default branch with a normal fast-forward push' \
    )
    _describe -t commands 'forge-cli help repo commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__repo__subcmd__bootstrap_commands] )) ||
_forge-cli__subcmd__help__subcmd__repo__subcmd__bootstrap_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help repo bootstrap commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__repo__subcmd__push-default_commands] )) ||
_forge-cli__subcmd__help__subcmd__repo__subcmd__push-default_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help repo push-default commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__repo__subcmd__view_commands] )) ||
_forge-cli__subcmd__help__subcmd__repo__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help repo view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__search_commands] )) ||
_forge-cli__subcmd__help__subcmd__search_commands() {
    local commands; commands=(
'issues:Full-text search over issues (\`gh search issues\`)' \
'prs:Full-text search over pull requests (\`gh search prs\`)' \
'refs-to:List issues / PRs that reference a ref via cross-reference events' \
    )
    _describe -t commands 'forge-cli help search commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__search__subcmd__issues_commands] )) ||
_forge-cli__subcmd__help__subcmd__search__subcmd__issues_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help search issues commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__search__subcmd__prs_commands] )) ||
_forge-cli__subcmd__help__subcmd__search__subcmd__prs_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help search prs commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__help__subcmd__search__subcmd__refs-to_commands] )) ||
_forge-cli__subcmd__help__subcmd__search__subcmd__refs-to_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli help search refs-to commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox_commands] )) ||
_forge-cli__subcmd__inbox_commands() {
    local commands; commands=(
'status:Summarize bounded personal work counts' \
'list:List normalized inbox items' \
'next:Return ranked next-action candidates' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli inbox commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__help_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__help_commands() {
    local commands; commands=(
'status:Summarize bounded personal work counts' \
'list:List normalized inbox items' \
'next:Return ranked next-action candidates' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli inbox help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__help__subcmd__list_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox help list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__help__subcmd__next_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__help__subcmd__next_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox help next commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__help__subcmd__status_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__help__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox help status commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__list_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__next_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__next_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox next commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__inbox__subcmd__status_commands] )) ||
_forge-cli__subcmd__inbox__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli inbox status commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue_commands] )) ||
_forge-cli__subcmd__issue_commands() {
    local commands; commands=(
'create:Open a new issue' \
'view:Fetch a single issue' \
'list:List issues filtered by state / labels / author / assignee' \
'edit:Mutate an issue' \
'comment:Append a comment to an issue' \
'close:Close an issue' \
'reopen:Reopen a closed issue' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli issue commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__close_commands] )) ||
_forge-cli__subcmd__issue__subcmd__close_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue close commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__comment_commands] )) ||
_forge-cli__subcmd__issue__subcmd__comment_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue comment commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__create_commands] )) ||
_forge-cli__subcmd__issue__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue create commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__edit_commands] )) ||
_forge-cli__subcmd__issue__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue edit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help_commands() {
    local commands; commands=(
'create:Open a new issue' \
'view:Fetch a single issue' \
'list:List issues filtered by state / labels / author / assignee' \
'edit:Mutate an issue' \
'comment:Append a comment to an issue' \
'close:Close an issue' \
'reopen:Reopen a closed issue' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli issue help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__close_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__close_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help close commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__comment_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__comment_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help comment commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__create_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help create commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__edit_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help edit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__list_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__reopen_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__reopen_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help reopen commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__help__subcmd__view_commands] )) ||
_forge-cli__subcmd__issue__subcmd__help__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue help view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__list_commands] )) ||
_forge-cli__subcmd__issue__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__reopen_commands] )) ||
_forge-cli__subcmd__issue__subcmd__reopen_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue reopen commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__issue__subcmd__view_commands] )) ||
_forge-cli__subcmd__issue__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli issue view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label_commands] )) ||
_forge-cli__subcmd__label_commands() {
    local commands; commands=(
'list:List repository labels through the selected provider backend' \
'audit:Compare repository labels against a machine-readable catalog' \
'ensure:Create missing labels and optionally update color / description drift' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli label commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__audit_commands] )) ||
_forge-cli__subcmd__label__subcmd__audit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label audit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__ensure_commands] )) ||
_forge-cli__subcmd__label__subcmd__ensure_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label ensure commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__help_commands] )) ||
_forge-cli__subcmd__label__subcmd__help_commands() {
    local commands; commands=(
'list:List repository labels through the selected provider backend' \
'audit:Compare repository labels against a machine-readable catalog' \
'ensure:Create missing labels and optionally update color / description drift' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli label help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__help__subcmd__audit_commands] )) ||
_forge-cli__subcmd__label__subcmd__help__subcmd__audit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label help audit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__help__subcmd__ensure_commands] )) ||
_forge-cli__subcmd__label__subcmd__help__subcmd__ensure_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label help ensure commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__label__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__help__subcmd__list_commands] )) ||
_forge-cli__subcmd__label__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label help list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__label__subcmd__list_commands] )) ||
_forge-cli__subcmd__label__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli label list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__operation-effect_commands] )) ||
_forge-cli__subcmd__operation-effect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli operation-effect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr_commands] )) ||
_forge-cli__subcmd__pr_commands() {
    local commands; commands=(
'create:Open a draft pull / merge request from the current branch' \
'view:Fetch a single PR / MR with normalised fields' \
'list:List PRs / MRs' \
'edit:Mutate PR / MR title, body, base, labels, reviewers' \
'comment:Append a comment to a PR / MR' \
'review:Post a review outcome comment and optionally mirror it to an issue' \
'comments:List the issue-style comment stream attached to a PR / MR' \
'ready:Promote a draft PR / MR to ready-for-review' \
'review-threads:List review threads attached to a PR / MR with their resolved state' \
'reviews:List native review summaries, classified against the current head' \
'pending-review:Recover an authenticated actor'\''s pending native review' \
'review-loop:Inspect and advance the durable review-loop ledger' \
'tasks:List GFM task-list items in the PR / MR description with their state' \
'merge:Merge a ready PR / MR' \
'close:Close a PR / MR without merging' \
'checks:One-shot snapshot of PR / MR check state' \
'wait-checks:Block until every required check reaches a terminal state' \
'deliver:End-to-end "open draft (or adopt the branch'\''s open PR) → CI green → ready → merge" macro' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__checks_commands] )) ||
_forge-cli__subcmd__pr__subcmd__checks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr checks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__close_commands] )) ||
_forge-cli__subcmd__pr__subcmd__close_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr close commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__comment_commands] )) ||
_forge-cli__subcmd__pr__subcmd__comment_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr comment commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__comments_commands] )) ||
_forge-cli__subcmd__pr__subcmd__comments_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr comments commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__create_commands] )) ||
_forge-cli__subcmd__pr__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr create commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__deliver_commands] )) ||
_forge-cli__subcmd__pr__subcmd__deliver_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr deliver commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__edit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr edit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help_commands() {
    local commands; commands=(
'create:Open a draft pull / merge request from the current branch' \
'view:Fetch a single PR / MR with normalised fields' \
'list:List PRs / MRs' \
'edit:Mutate PR / MR title, body, base, labels, reviewers' \
'comment:Append a comment to a PR / MR' \
'review:Post a review outcome comment and optionally mirror it to an issue' \
'comments:List the issue-style comment stream attached to a PR / MR' \
'ready:Promote a draft PR / MR to ready-for-review' \
'review-threads:List review threads attached to a PR / MR with their resolved state' \
'reviews:List native review summaries, classified against the current head' \
'pending-review:Recover an authenticated actor'\''s pending native review' \
'review-loop:Inspect and advance the durable review-loop ledger' \
'tasks:List GFM task-list items in the PR / MR description with their state' \
'merge:Merge a ready PR / MR' \
'close:Close a PR / MR without merging' \
'checks:One-shot snapshot of PR / MR check state' \
'wait-checks:Block until every required check reaches a terminal state' \
'deliver:End-to-end "open draft (or adopt the branch'\''s open PR) → CI green → ready → merge" macro' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__checks_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__checks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help checks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__close_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__close_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help close commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__comment_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__comment_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help comment commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__comments_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__comments_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help comments commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__create_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help create commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__deliver_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__deliver_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help deliver commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__edit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help edit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__list_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__merge_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__merge_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help merge commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review_commands() {
    local commands; commands=(
'inspect:Read one complete pending-review snapshot and immutable digest' \
'resume-submit:Resume and submit an exact receipt-bound review transaction' \
'submit:Guardedly submit an unmarked pending review' \
'discard:Destructively discard one exact pending review' \
'delete:Verify and delete one exact pending review node' \
    )
    _describe -t commands 'forge-cli pr help pending-review commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__delete_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__delete_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help pending-review delete commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__discard_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__discard_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help pending-review discard commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help pending-review inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__resume-submit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__resume-submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help pending-review resume-submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__submit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__pending-review__subcmd__submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help pending-review submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__ready_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__ready_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help ready commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review_commands() {
    local commands; commands=(
'validate:Validate review summary and thread specs before posting' \
    )
    _describe -t commands 'forge-cli pr help review commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review__subcmd__validate_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop_commands() {
    local commands; commands=(
'inspect:Inspect the validated ledger tip and current round state' \
'observe:Observe one reviewed head and append the resulting state transition' \
'extend:Consume an exact provider-visible approval to extend one budget' \
'validate:Validate a findings payload offline, before it reaches the provider' \
    )
    _describe -t commands 'forge-cli pr help review-loop commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__extend_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__extend_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-loop extend commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-loop inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__observe_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__observe_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-loop observe commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__validate_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-loop__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-loop validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads_commands() {
    local commands; commands=(
'list:List review threads attached to a PR / MR with their resolved state' \
'resolve:Resolve a review thread, optionally posting a reply first (GitHub)' \
'reply:Reply to a review thread without resolving it (GitHub)' \
    )
    _describe -t commands 'forge-cli pr help review-threads commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads__subcmd__list_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-threads list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads__subcmd__reply_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads__subcmd__reply_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-threads reply commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads__subcmd__resolve_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__review-threads__subcmd__resolve_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help review-threads resolve commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__reviews_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__reviews_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help reviews commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__tasks_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__tasks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help tasks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__view_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__help__subcmd__wait-checks_commands] )) ||
_forge-cli__subcmd__pr__subcmd__help__subcmd__wait-checks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr help wait-checks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__list_commands] )) ||
_forge-cli__subcmd__pr__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__merge_commands] )) ||
_forge-cli__subcmd__pr__subcmd__merge_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr merge commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review_commands() {
    local commands; commands=(
'inspect:Read one complete pending-review snapshot and immutable digest' \
'resume-submit:Resume and submit an exact receipt-bound review transaction' \
'submit:Guardedly submit an unmarked pending review' \
'discard:Destructively discard one exact pending review' \
'delete:Verify and delete one exact pending review node' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr pending-review commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__delete_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__delete_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review delete commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__discard_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__discard_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review discard commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help_commands() {
    local commands; commands=(
'inspect:Read one complete pending-review snapshot and immutable digest' \
'resume-submit:Resume and submit an exact receipt-bound review transaction' \
'submit:Guardedly submit an unmarked pending review' \
'discard:Destructively discard one exact pending review' \
'delete:Verify and delete one exact pending review node' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr pending-review help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__delete_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__delete_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review help delete commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__discard_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__discard_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review help discard commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review help inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__resume-submit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__resume-submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review help resume-submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__submit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__help__subcmd__submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review help submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__resume-submit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__resume-submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review resume-submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__submit_commands] )) ||
_forge-cli__subcmd__pr__subcmd__pending-review__subcmd__submit_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr pending-review submit commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__ready_commands] )) ||
_forge-cli__subcmd__pr__subcmd__ready_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr ready commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review_commands() {
    local commands; commands=(
'validate:Validate review summary and thread specs before posting' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr review commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review__subcmd__help_commands() {
    local commands; commands=(
'validate:Validate review summary and thread specs before posting' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr review help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review__subcmd__help__subcmd__validate_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review__subcmd__help__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review help validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review__subcmd__validate_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop_commands() {
    local commands; commands=(
'inspect:Inspect the validated ledger tip and current round state' \
'observe:Observe one reviewed head and append the resulting state transition' \
'extend:Consume an exact provider-visible approval to extend one budget' \
'validate:Validate a findings payload offline, before it reaches the provider' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr review-loop commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__extend_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__extend_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop extend commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help_commands() {
    local commands; commands=(
'inspect:Inspect the validated ledger tip and current round state' \
'observe:Observe one reviewed head and append the resulting state transition' \
'extend:Consume an exact provider-visible approval to extend one budget' \
'validate:Validate a findings payload offline, before it reaches the provider' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr review-loop help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__extend_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__extend_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop help extend commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop help inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__observe_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__observe_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop help observe commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__validate_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__help__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop help validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__inspect_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop inspect commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__observe_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__observe_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop observe commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__validate_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-loop__subcmd__validate_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-loop validate commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads_commands() {
    local commands; commands=(
'list:List review threads attached to a PR / MR with their resolved state' \
'resolve:Resolve a review thread, optionally posting a reply first (GitHub)' \
'reply:Reply to a review thread without resolving it (GitHub)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr review-threads commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help_commands() {
    local commands; commands=(
'list:List review threads attached to a PR / MR with their resolved state' \
'resolve:Resolve a review thread, optionally posting a reply first (GitHub)' \
'reply:Reply to a review thread without resolving it (GitHub)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli pr review-threads help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__list_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads help list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__reply_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__reply_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads help reply commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__resolve_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__help__subcmd__resolve_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads help resolve commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__list_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__reply_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__reply_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads reply commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__resolve_commands] )) ||
_forge-cli__subcmd__pr__subcmd__review-threads__subcmd__resolve_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr review-threads resolve commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__reviews_commands] )) ||
_forge-cli__subcmd__pr__subcmd__reviews_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr reviews commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__tasks_commands] )) ||
_forge-cli__subcmd__pr__subcmd__tasks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr tasks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__view_commands] )) ||
_forge-cli__subcmd__pr__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__pr__subcmd__wait-checks_commands] )) ||
_forge-cli__subcmd__pr__subcmd__wait-checks_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli pr wait-checks commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider_commands] )) ||
_forge-cli__subcmd__provider_commands() {
    local commands; commands=(
'add:Add a named provider record without storing its token value' \
'list:List named provider records' \
'view:View one named provider record' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli provider commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__add_commands] )) ||
_forge-cli__subcmd__provider__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider add commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__help_commands] )) ||
_forge-cli__subcmd__provider__subcmd__help_commands() {
    local commands; commands=(
'add:Add a named provider record without storing its token value' \
'list:List named provider records' \
'view:View one named provider record' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli provider help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__help__subcmd__add_commands] )) ||
_forge-cli__subcmd__provider__subcmd__help__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider help add commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__provider__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__help__subcmd__list_commands] )) ||
_forge-cli__subcmd__provider__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider help list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__help__subcmd__view_commands] )) ||
_forge-cli__subcmd__provider__subcmd__help__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider help view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__list_commands] )) ||
_forge-cli__subcmd__provider__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider list commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__provider__subcmd__view_commands] )) ||
_forge-cli__subcmd__provider__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli provider view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo_commands] )) ||
_forge-cli__subcmd__repo_commands() {
    local commands; commands=(
'view:Resolve the repo slug, default branch, and supported merge methods' \
'bootstrap:Create and initialize one new private Forgejo repository under a durable receipt' \
'push-default:Deliver one signed commit to the default branch with a normal fast-forward push' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli repo commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__bootstrap_commands] )) ||
_forge-cli__subcmd__repo__subcmd__bootstrap_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo bootstrap commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__help_commands] )) ||
_forge-cli__subcmd__repo__subcmd__help_commands() {
    local commands; commands=(
'view:Resolve the repo slug, default branch, and supported merge methods' \
'bootstrap:Create and initialize one new private Forgejo repository under a durable receipt' \
'push-default:Deliver one signed commit to the default branch with a normal fast-forward push' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli repo help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__help__subcmd__bootstrap_commands] )) ||
_forge-cli__subcmd__repo__subcmd__help__subcmd__bootstrap_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo help bootstrap commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__repo__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__help__subcmd__push-default_commands] )) ||
_forge-cli__subcmd__repo__subcmd__help__subcmd__push-default_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo help push-default commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__help__subcmd__view_commands] )) ||
_forge-cli__subcmd__repo__subcmd__help__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo help view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__push-default_commands] )) ||
_forge-cli__subcmd__repo__subcmd__push-default_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo push-default commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__repo__subcmd__view_commands] )) ||
_forge-cli__subcmd__repo__subcmd__view_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli repo view commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search_commands] )) ||
_forge-cli__subcmd__search_commands() {
    local commands; commands=(
'issues:Full-text search over issues (\`gh search issues\`)' \
'prs:Full-text search over pull requests (\`gh search prs\`)' \
'refs-to:List issues / PRs that reference a ref via cross-reference events' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli search commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__help_commands] )) ||
_forge-cli__subcmd__search__subcmd__help_commands() {
    local commands; commands=(
'issues:Full-text search over issues (\`gh search issues\`)' \
'prs:Full-text search over pull requests (\`gh search prs\`)' \
'refs-to:List issues / PRs that reference a ref via cross-reference events' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'forge-cli search help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__help__subcmd__help_commands] )) ||
_forge-cli__subcmd__search__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search help help commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__help__subcmd__issues_commands] )) ||
_forge-cli__subcmd__search__subcmd__help__subcmd__issues_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search help issues commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__help__subcmd__prs_commands] )) ||
_forge-cli__subcmd__search__subcmd__help__subcmd__prs_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search help prs commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__help__subcmd__refs-to_commands] )) ||
_forge-cli__subcmd__search__subcmd__help__subcmd__refs-to_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search help refs-to commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__issues_commands] )) ||
_forge-cli__subcmd__search__subcmd__issues_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search issues commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__prs_commands] )) ||
_forge-cli__subcmd__search__subcmd__prs_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search prs commands' commands "$@"
}
(( $+functions[_forge-cli__subcmd__search__subcmd__refs-to_commands] )) ||
_forge-cli__subcmd__search__subcmd__refs-to_commands() {
    local commands; commands=()
    _describe -t commands 'forge-cli search refs-to commands' commands "$@"
}

if [ "$funcstack[1]" = "_forge-cli" ]; then
    _forge-cli "$@"
else
    compdef _forge-cli forge-cli
fi

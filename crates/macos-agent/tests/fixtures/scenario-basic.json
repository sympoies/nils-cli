{
  "steps": [
    {
      "id": "preflight",
      "args": ["--format", "json", "preflight"]
    },
    {
      "id": "activate",
      "args": ["--format", "json", "window", "activate", "--app", "Terminal", "--wait-ms", "25"]
    },
    {
      "id": "click-dry-run",
      "args": ["--format", "json", "--dry-run", "input", "click", "--x", "10", "--y", "10"]
    }
  ]
}

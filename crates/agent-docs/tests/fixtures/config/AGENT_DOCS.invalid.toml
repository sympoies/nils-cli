# Invalid fixture for AGENT_DOCS schema validation tests.
# Contains multiple independent failures:
# - invalid context
# - missing required path
# - invalid required type
# - unsupported when

[[document]]
context = "project"
scope = "project"
path = "BINARY_DEPENDENCIES.md"
required = true
when = "always"
notes = "invalid context name"

[[document]]
context = "startup"
scope = "home"
required = true
when = "always"
notes = "missing path key"

[[document]]
context = "skill-dev"
scope = "home"
path = "DEVELOPMENT.md"
required = "yes"
when = "always"
notes = "required must be boolean"

[[document]]
context = "task-tools"
scope = "home"
path = "CLI_TOOLS.md"
required = true
when = "if-env:CI"
notes = "unsupported when condition"

[package]
name = "agent-provider-gemini"
version = "0.3.0"
edition.workspace = true
license = "MIT OR Apache-2.0"

[dependencies]
agent-runtime-core = { path = "../agent-runtime-core" }
serde_json = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }

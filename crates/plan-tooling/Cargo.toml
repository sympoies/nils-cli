[package]
name = "nils-plan-tooling"
version = "0.4.2"
edition.workspace = true
license.workspace = true

description = "CLI crate for nils-plan-tooling in the nils-cli workspace."
repository = "https://github.com/graysurf/nils-cli"

[lib]
name = "plan_tooling"
path = "src/lib.rs"

[[bin]]
name = "plan-tooling"
path = "src/main.rs"
[dependencies]
anyhow = { workspace = true }
nils-term = { version = "0.4.2", path = "../nils-term", package = "nils-term" }
nils-common = { version = "0.4.2", path = "../nils-common", package = "nils-common" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
nils-test-support = { version = "0.4.2", path = "../nils-test-support" }
pretty_assertions = { workspace = true }
tempfile = "3"
